Updates the timezones guide and dimensions reference based on [`docs/timezone-handling.md` in lightdash/lightdash](https://github.com/lightdash/lightdash/blob/main/docs/timezone-handling.md) and [PR #22762](https://github.com/lightdash/lightdash/pull/22762).

### `guides/developer/timezones.mdx`
- Reframed "Timezone settings" around the two settings (project + data) up front.
- Expanded **Project query timezone** to cover its full scope: relative date filters, `DATE_TRUNC` grouping, EXTRACT-based intervals, result display (table cells, chart axes, tooltips, CSV/Excel/Sheets exports, resolved-TZ badge), and the DATE-base-dimension bypass.
- Added **Per-dimension display opt-out** section documenting `convert_timezone: false` (Beta callout).
- Added **Data timezone** section with Experimental callout (`EnableTimezoneSupport`), session-command behavior, BigQuery/Athena gap, and the warehouse support table.
- Updated workarounds to point at the data timezone and `convert_timezone: false` where relevant.
- Updated summary table.

### `references/dimensions.mdx`
- Added `convert_timezone` row to the dimension properties table.
- Added a **Convert timezone** section linking back to the timezones guide.

Maturity callouts mirror the format used in `guides/data-apps`, `references/workspace/user-impersonation`, and the existing `Custom granularities` section.