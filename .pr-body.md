Adds a new `guides/data-apps.mdx` page covering the Data apps feature, and links it under the **Build with AI** section in `docs.json`.

The page covers:
- **Creating a new app** — where to start, choosing a template, attaching charts/dashboards/images as context, sample data toggle, iteration, and the query inspector.
- **Data context and the app model** — how queries are proxied through Lightdash with the viewer's permissions, and what the agent can and can't see.
- **Sharing an app** — moving apps from personal into spaces; access then follows space roles.
- **Best practices** — iterating across multiple prompts, using images for style, sample data trade-offs, referencing existing charts, and using the query inspector.

Source context pulled from `lightdash/lightdash/docs/data-apps.md`. Avoids the "lovable" working title per request.