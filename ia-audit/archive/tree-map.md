# Lightdash docs — IA tree map (current state)

Generated 2026-07-28 by an audit team; 218 pages; per-page annotations come from full-page reads. Legend: **type** is the Diataxis-style classification (tutorial/how-to/reference/explanation/mixed); **audience** is one of explorer/builder/admin/platform/app-dev/ai-builder.

## Docs

### Introduction

- **introduction** — "Welcome to Lightdash" · explanation · explorer · 164w · 0 inbound
  - One-paragraph marketing-style landing page framing Lightdash as an "Agentic Analytics" platform built on a "Context Layer" that "knows everything about your data and your business" (term is never defined here or linked anywhere); embeds a YouTube intro video and a Slack community invite link.
  - 🚩 flags: stub — 164 words, no concrete product detail, and introduces "Context Layer" terminology that isn't explained or cross-linked from this page
- **best-practice/lightdash-way** — "The Lightdash way: data access" · explanation · builder · 632w · 1 inbound
  - Lays out "The Lightdash Way" philosophy for data access — contrasts giving everyone raw-data access, funneling all questions through analysts, and (the recommended path) data builders publishing metrics + dimensions as reusable building blocks — with pros/cons for each approach.
- **Setting up a new project**
  - **get-started/setup-lightdash/intro** — "Setting up a new project" · reference · admin · 33w · 1 inbound
    - Section landing page for "Setting up a new project" — a CardGroup of three links (create first project, update connection, invite team) with no body content of its own.
    - 🚩 flags: stub — pure navigation card group, no original content
  - **get-started/setup-lightdash/get-project-lightdash-ready** — "Create your first project" · tutorial · builder · 1082w · 3 inbound
    - First-project tutorial: install Node/npm (with an NVM fallback accordion), install the Lightdash CLI via `npm install -g @lightdash/cli`, log in with a generated personal access token, run `lightdash dbt run` (optionally scoped with `-s tag:lightdash` / model selectors) to generate Tables/dimensions from dbt YAML, then `lightdash deploy --create`.
    - ⚠ overlaps: get-started/develop-in-lightdash/adding-tables-to-lightdash — same "What are Tables?" explanation and the identical dbt-tag/model-selector code block (`lightdash dbt run -s tag:lightdash`, `-s payments+`, etc.) | guides/cli/how-to-install-the-lightdash-cli — duplicates the npm/NVM install steps
  - **get-started/setup-lightdash/connect-project** — "Update your project connection" · reference · admin · 8965w · 8 inbound
    - The canonical, field-by-field connection reference: warehouse setup for BigQuery, Postgres, Supabase (including a quickstart-repo path with Supabase MCP + AI-agent model generation), Redshift (password, AWS IAM CLI credentials, and IAM Identity Center SSO, each with policy/setup accordions), Snowflake (including OAuth security-integration setup), Databricks (including OAuth app setup), Trino, ClickHouse (with a boolean-column known-issue workaround), DuckDB (MotherDuck and DuckLake catalog/data-path backends with full dbt profile examples), and Athena; then dbt project import via GitHub (OAuth/PAT/fine-grained token), GitLab, Azure DevOps, Bitbucket, dbt Cloud (service token, webhook secret/HMAC verification, region-specific Discovery API endpoints), CLI, and local dbt project.
    - ⚠ overlaps: none substantive — this is the source others link to rather than duplicate
    - 🚩 flags: none (note: at ~8,965 words this is by far the largest page in the docs set — a candidate for splitting per-warehouse into separate pages for navigability, though not a correctness issue)
  - **get-started/setup-lightdash/invite-new-users** — "Inviting teammates to your Lightdash project" · how-to · admin · 389w · 2 inbound
    - Covers allowed-email-domain auto-join (with per-project default role assignment and an org-wide default project setting) and manually inviting individual users by email with a role, plus viewing/regenerating pending invites.

### Explore and analyze

- **Lightdash BI quickstart**
  - **get-started/exploring-data/intro** — "Learning to explore data in Lightdash" · reference · explorer · 175w · 0 inbound
    - Section landing page for the BI quickstart — CardGroup linking to the six exploring-data sub-pages, plus two embedded Loom videos (fundamentals; advanced functionality like custom metrics/table calcs) that reference the public demo.lightdash.com dataset.
  - **get-started/exploring-data/exploring-your-content** — "Exploring your content" · tutorial · explorer · 1062w · 2 inbound
    - Covers browsing saved content from the homepage/Browse menu, using global search (Cmd/Ctrl+K) with content-type/date/creator filters and grouped results, viewing underlying data behind a chart point or table cell, and drilling into a metric by a dimension via the "drill by" action menu.
    - ⚠ overlaps: get-started/exploring-data/interactive-dashboards — near-duplicate "drill into a metric" walkthrough (same feature/steps, different sample data and screenshots) | get-started/exploring-data/using-explores — both cover "Explore from here" / entering the Explore view from existing content
  - **get-started/exploring-data/intro-metrics-dimensions** — "An intro to metrics and dimensions" · explanation · explorer · 117w · 2 inbound
    - One-screen conceptual intro: dimensions segment data, metrics calculate on data, illustrated with an ecommerce orders example (order count metric split by order-status dimension); links out to the Lightdash Way page for rationale.
    - ⚠ overlaps: get-started/develop-in-lightdash/how-to-create-dimensions and how-to-create-metrics — the "segment data / perform calculations" definitions are copy-pasted verbatim into both the consumer-facing and builder-facing versions
  - **get-started/exploring-data/using-explores** — "Querying from tables and exploring your data" · tutorial · explorer · 1006w · 6 inbound
    - Walks through the Explore view's five panes (metrics/dimensions, filters, chart, results, SQL), running a query (select metric + dimension(s) + Run query), three ways to add filters, sorting including per-field NULL-first/last/default ordering, building a chart, saving to a Dashboard vs. a Space (with reuse tradeoffs explained), and changing a saved chart's underlying explore via "Change explore" (with a bulk "update all other charts using this explore" option and dropped/kept-field remapping rules).
    - ⚠ overlaps: get-started/exploring-data/exploring-your-content — both describe "Explore from here" as an entry point
  - **get-started/exploring-data/sharing-insights** — "Sharing with your team" · how-to · explorer · 309w · 1 inbound
    - Three sharing mechanisms: copying a shortened draft-exploration URL, saving/sharing a chart URL (access gated by the destination Space's permissions), and downloading chart images (JPEG/PNG/SVG/PDF/JSON, opaque or transparent background) or results (CSV/Excel/Google Sheets export).
  - **best-practice/planning-your-dashboard** — "Planning your dashboard: the Who, What and How of building great dashboards." · how-to · explorer · 308w · 0 inbound
    - Pre-build planning framework for dashboards: answer who the dashboard is for, what it should accomplish (drive an action vs. inform), and how to build it (matching datasets/metrics to the outline), each with a short action checklist; includes an embedded 5-minute video walkthrough.
  - **get-started/exploring-data/dashboards** — "Creating dashboards" · tutorial · explorer · 1647w · 4 inbound
    - End-to-end dashboard build: create dashboard, add tiles (new chart exclusive to the dashboard vs. an existing saved chart, resize/reposition), add markdown tiles (responsive-image CSS technique, an accordion of raw-HTML formatting tricks) and Loom video tiles, save, add filters (empty-default filters for viewer self-service, and "require viewers to pick a value" behavior that clears any default), add/rename/remove/transfer-content tabs (with a callout on when tabs make sense) and note that embedding a tabbed dashboard flattens all tabs into one, hide tile headers, and share via URL.
    - ⚠ overlaps: get-started/exploring-data/interactive-dashboards — this page documents adding dashboard filters, that page documents using them (cross-filtering) — complementary, not duplicated
  - **get-started/exploring-data/interactive-dashboards** — "Using dashboards" · tutorial · explorer · 643w · 3 inbound
    - Viewer-side dashboard interactions: viewing underlying data behind a chart point, cross-filtering (click a chart value to filter every other tile, with a hover indicator showing which tiles were filtered), date zoom (temporary granularity override that reverts on refresh, with a pointer to the full guide), and drilling into a metric by a dimension via the action menu.
    - ⚠ overlaps: get-started/exploring-data/exploring-your-content — near-verbatim duplicate "drill into" walkthrough (same steps, different sample chart)
- **BI feature guides**
  - **Metrics**
    - **guides/metrics-catalog** — "Metrics" · reference · builder · 258w · 3 inbound
      - Landing page for the Metrics section (Catalog vs. Saved Trees toggle) and an overview of the `spotlight` YAML namespace — the single config key covering catalog visibility, categories, ownership, and explorer defaults, settable project-wide in `lightdash.config.yml` or per-metric/model under `meta.spotlight`.
    - **guides/metrics-catalog/catalog** — "Catalog" · how-to · builder · 3495w · 1 inbound
      - Browsing/searching/exploring metrics in the Metrics Explorer (time dimension plot, date-range presets by granularity, dimension filter/segment-by, "compare to" same-table metric or prior year, drag-to-zoom); then extensive `spotlight`-key YAML curation: global/per-metric visibility, categories (with color enum), metric/model-level ownership (email-based, metric overrides model default), dimension-level `filter_by`/`segment_by` hide flags vs. metric-level allowlists that override them, `default_segment`/`default_filter` pre-selection, and table-/metric-level `default_time_dimension`; ends with a roles-and-permissions matrix.
      - ⚠ overlaps: guides/metrics-catalog — complementary (that page introduces the `spotlight` namespace; this page is its detailed implementation), not duplicated
    - **guides/metrics-catalog/saved-trees** — "Saved Trees" · how-to · explorer · 604w · 2 inbound
      - Building persistent, shareable metric-relationship diagrams on a canvas (drag metrics from a searchable sidebar, draw edges, save with name + at least one node); collaborative single-editor locking with a 2-minute idle auto-release and draft auto-save for crash recovery; permissions table showing only Interactive Viewer/Viewer are read-only.
      - ⚠ overlaps: guides/metrics-catalog/drivers — see that page's note
    - **guides/metrics-catalog/drivers** — "Drivers" · reference · builder · 399w · 2 inbound
      - YAML `drivers` property on a metric lists the metrics that feed into it (same-table by name, or cross-table via `table.metric` syntax), auto-drawing directed edges on the Saved Trees canvas; driver edges are automatic and read-only in the UI (can only be removed by deleting a node).
      - ⚠ overlaps: guides/metrics-catalog/saved-trees — both state that driver edges are not manually removable/stored and are injected automatically; minor restatement of the same fact in each doc, not problematic
  - **Chart types**
    - **references/chart-types/overview** — "Chart types overview" · reference · explorer · 145w · 1 inbound
      - Flat bullet-list index of all 15 built-in chart types plus a link to data app visualizations, with one screenshot of the chart-type dropdown in the Configure panel.
      - 🚩 flags: stub — 145 words, pure link list duplicating the sidebar nav structure, adds no info beyond titles
    - **references/chart-types/bar-chart** — "Bar chart" · reference · explorer · 321w · 1 inbound
      - When to use bar charts (comparing groups, tracking values over time with few x-axis values); covers stacked and 100% stacked bar charts, including the 100% stacking toggle and an alternate table-calculation-based method.
      - ⚠ overlaps: guides/table-calculations/table-calculation-sql-templates/percent-of-group-pivot-total — describes the same manual 100%-stack calculation as an alternative to the built-in toggle
    - **references/chart-types/horizontal-bar-chart** — "Horizontal bar chart" · reference · explorer · 62w · 1 inbound
      - Explains horizontal bar charts are vertical bar charts rotated, useful for many or long category labels; no unique config beyond a link to configure-charts.
      - 🚩 flags: stub — 62 words, minimal unique content beyond bar-chart.mdx's framing
    - **references/chart-types/line-chart** — "Line chart" · reference · explorer · 114w · 1 inbound
      - Line charts suit small value changes (y-axis needn't start at zero) or many x-axis points; multi-line charts compare groups over the same period.
    - **references/chart-types/area-chart** — "Area chart" · reference · explorer · 221w · 1 inbound
      - Guidance on choosing area vs. line/bar/stacked-bar charts based on whether totals matter, value-magnitude differences, and data point count.
    - **references/chart-types/mixed-chart** — "Mixed chart" · reference · explorer · 113w · 1 inbound
      - Mixed charts combine bar/line/scatter series on one chart; requires starting from a line, scatter, or horizontal-bar chart type with 2+ series, configured per-series in the series tab.
    - **references/chart-types/scatter-chart** — "Scatter chart" · reference · explorer · 80w · 1 inbound
      - Scatter charts show correlation between two variables; an optional third grouping variable colors points by group.
    - **references/chart-types/pie-chart** — "Pie chart" · reference · explorer · 228w · 2 inbound
      - Pie/donut toggle; requires one metric + one dimension (multiple dimensions for more granular groups); documents the `${table_field.granularity}` label placeholder tied to date zoom, with a warning that date-zoom re-bucketing breaks per-slice label overrides.
      - ⚠ overlaps: references/chart-types/funnel-chart, references/chart-types/gauge, references/chart-types/big-value, guides/configure-charts — near-identical "referencing active granularity in labels" explanation and date-zoom warning repeated across all
    - **references/chart-types/funnel-chart** — "Funnel chart" · reference · explorer · 341w · 3 inbound
      - Row vs. column data orientation for defining funnel stages (first column/row = labels, rest = numeric step values); documents the same granularity-placeholder/date-zoom label feature as pie/gauge/big-value.
      - ⚠ overlaps: references/chart-types/pie-chart, references/chart-types/gauge, references/chart-types/big-value, guides/configure-charts — granularity placeholder section; references/chart-types/custom-charts — funnel template section explicitly says "use the built-in Funnel chart" for standard cases (intentional cross-reference, not duplication)
    - **references/chart-types/treemap-chart** — "Treemap chart" · reference · explorer · 239w · 1 inbound
      - Single/two/3+ dimension treemap behavior (rectangle sizing, nesting, click-to-drill-down); Layout settings (dimension hierarchy, size/color metric) and Display settings (minimum section size, max leaf depth default 2).
    - **references/chart-types/sankey** — "Sankey chart" · reference · explorer · 577w · 2 inbound
      - Beta feature. Three-column data requirement (source, target, weight); multi-step and cyclical flow handling (auto "Step N" node suffixing for loops); three node-layout modes (Multi-step default, Merged for acyclic-only, Direct two-column).
      - ⚠ overlaps: references/chart-types/custom-charts — custom-charts' "Sankey-style bar chart" template explicitly differentiates itself as an escape hatch, not a duplicate
    - **references/chart-types/table** — "Table" · reference · explorer · 3348w · 2 inbound
      - Largest chart-type reference. Covers columns/rows/metrics basics, tiny bars, row-limiting controls (Show/Hide, First/Last, N), pivot configuration (Columns/Rows/Metrics lists, header-click sorting, hidden dimensions still driving sort/grouping, 200-column default limit with `LIGHTDASH_PIVOT_TABLE_MAX_COLUMN_LIMIT` env var, metrics-as-rows vs. metrics-as-columns math), totals (including per-table-calculation totals modes: Formula/Sum of rows/None), subtotals with expand-by-default, row-value grouping, column freeze/resize, and conditional formatting (highlighting + color ranges).
      - ⚠ overlaps: guides/configure-charts — general "Limit displayed rows"/"Column limit" concepts overlap with this page's table-specific row-limiting and pivot column-limit sections; references/table-calculation-functions/aggregate-functions — the "Incorrect totals" section's manual `SUM(...) OVER()` workaround duplicates what `total()` now does natively (and does correctly for count_distinct, unlike the manual SQL shown here)
    - **references/chart-types/big-value** — "Big value" · reference · explorer · 397w · 2 inbound
      - Big-value field selection, previous-row comparison (raw value or percentage change, red/green coloring), the granularity-placeholder label feature, and conditional formatting rules (numeric/string operators, AND-combined conditions within a rule, multiple rules with last-wins priority, separate light/dark colors).
      - ⚠ overlaps: references/chart-types/pie-chart, references/chart-types/funnel-chart, references/chart-types/gauge, guides/configure-charts — granularity placeholder section; references/chart-types/table — both have "Conditional formatting" sections but with different mechanics (whole-value color vs. per-cell/range highlighting), so not true duplication
    - **references/chart-types/gauge** — "Gauge" · reference · explorer · 163w · 2 inbound
      - Configuring a gauge (single metric value, optional min/max bounds, colored threshold ranges like red/amber/green); documents the granularity-placeholder label feature for custom value/percentage labels.
      - ⚠ overlaps: references/chart-types/pie-chart, references/chart-types/funnel-chart, references/chart-types/big-value, guides/configure-charts — granularity placeholder section
    - **references/chart-types/map** — "Map" · reference · explorer · 1040w · 1 inbound
      - Three map modes (scatter, choropleth/area, heatmap), each with data-requirement and layout/display option tables; details custom GeoJSON join-field configuration and the server-side proxy's URL requirements (HTTPS, .json/.geojson/.topojson extension, 10MB/30s limits, public-IP-only, no redirects followed).
    - **references/chart-types/custom-charts** — "Custom charts" · reference · builder · 2869w · 1 inbound
      - Vega-Lite-powered custom charts; lists limitations (no drill-down, no view-underlying-data, no cross-filtering); quickstart plus 8 full Vega-Lite JSON template examples (bar w/ pivot, sankey-style stacked bar, confidence-interval line, heatmap, bubble plot, funnel, waterfall, trellis area, highlighted bar) with live demo links.
      - ⚠ overlaps: references/chart-types/sankey, references/chart-types/funnel-chart — explicitly cross-referenced as "use the built-in chart instead for standard cases," intentional differentiation rather than duplication
      - 🚩 flags: naming — page demands writing/editing raw Vega-Lite JSON, a builder-level task, yet sits in the same explorer-facing nav group as the point-and-click chart types
  - **guides/configure-charts** — "Configure your charts" · reference · explorer · 2012w · 7 inbound
    - Chart config panel reference covering Layout, Series (including per-metric conditional bar coloring rules and category coloring), Axes (granularity placeholder `${table_field.granularity}`, scrollable-chart window settings), Display (reference lines with dynamic min/max/median/average, legend placement and persisted legend-selection stored in `echartsConfig.legend.selected`), Data (client-side row limit and pivot column limit that don't affect totals/exports), and Margins.
    - ⚠ overlaps: guides/date-zoom — both describe the `${table_field.granularity}` label placeholder (cross-linked, not duplicated); references/chart-types/table — column limit cross-references pivot table column-limit reference (link only)
  - **guides/how-to-create-scheduled-deliveries** — "Scheduled deliveries" · how-to · explorer · 1956w · 11 inbound
    - Creating scheduled deliveries (admin/developer/editor/interactive-viewer roles) with Setup (cadence, cron, timezone, recipients), Data & format (Image/CSV/xlsx/PDF, one-PDF-page-per-tab option), Message vs. AI-agent-generated message (Cloud Pro/Enterprise add-on), the admin-only overview page (delivery status, run details, single/bulk owner reassignment), Teams/Google Chat webhook setup, project default timezone, and configurable download-link expiration (per-channel overrides, persistent download URLs for expiries over 7 days).
    - ⚠ overlaps: guides/how-to-create-alerts — see above
  - **guides/how-to-create-alerts** — "Alerts" · how-to · explorer · 836w · 3 inbound
    - Creating chart alerts (admin/developer/edit-access only) with conditions (is greater/less than, increased/decreased by %), run frequency (UTC), and Slack/email destinations including private Slack channels and Microsoft Teams webhooks; delivered files expire after 3 days.
    - ⚠ overlaps: guides/how-to-create-scheduled-deliveries — near-duplicate instructions for enabling Slack, sending to private Slack channels, and sending to Microsoft Teams (this page has a condensed generic MS Teams webhook walkthrough; scheduled-deliveries has a fuller Power Automate–specific version)
  - **guides/using-slack-integration** — "Using the Slack integration" · how-to · explorer · 70w · 4 inbound
    - One-paragraph intro pointing to a walkthrough video and to the Slack integration reference doc for the full feature list; requires an org admin to have added Slack to Lightdash first.
    - 🚩 flags: stub — almost entirely a video embed with a single sentence of surrounding text; all substantive feature documentation lives at references/integrations/slack-integration
  - **guides/limiting-data-using-filters** — "Using Filters" · how-to · explorer · 3336w · 6 inbound
    - Adding filters in the Explore view (Filters tab, sidebar, results table, nested filter groups, CSV upload for bulk string values) and on dashboards (temporary vs. saved filters, cross-filtering, SQL-Runner-chart filtering by column), dashboard filter-vs-chart-filter override behavior, required filters and "at least one of" requirement groups (with YAML `required`/`requiredGroupId`/`requiredFiltersNote`), empty/unset default filters, sharing dashboards with filter overrides, and cross-table filter-value autocomplete.
    - ⚠ overlaps: guides/filters — see naming flag on that page
    - 🚩 flags: naming — see guides/filters
  - **guides/filters** — "Filters reference" · reference · explorer · 2217w · 1 inbound
    - Filter-type operator reference tables (numeric, string, boolean, date) plus an extensive BigQuery-syntax reference of compiled SQL for every relative-date filter combination (current/last/next periods, completed vs. rolling, custom ranges) for both timestamp and date fields.
    - ⚠ overlaps: guides/limiting-data-using-filters — see naming flag below; not duplicated content (that page covers UI usage, this page covers operator/SQL reference), but the pairing is confusing
    - 🚩 flags: naming — this page's slug/title ("Filters reference" at `/guides/filters`) reads like it should be the primary filters guide, while the actual how-to content for using filters lives at the oddly-named `/guides/limiting-data-using-filters` ("Using Filters"); each page links to the other by generic phrasing ("check out our filters reference doc" / "check out our docs on limiting data using filters") without making the reference-vs-how-to distinction obvious
  - **guides/date-zoom** — "Date zoom" · mixed(how-to+reference) · explorer · 1529w · 11 inbound
    - Dashboard date-zoom controls (Default zoom pill plus named controls, each governing a chosen set of charts via a specific date field, one control per chart), configuring available/default granularities, custom granularities from `lightdash.config.yml`, hide/show toggles, the reserved SQL parameter `${ld.parameters.date_zoom}`, the `${table_field.granularity}` label placeholder (with a caveat that pie/funnel per-category label overrides silently stop matching after a granularity change), and the `canDateZoom` embed JWT option.
    - ⚠ overlaps: guides/configure-charts — shared granularity-placeholder syntax (cross-linked, not duplicated)
  - **guides/formatting-your-fields** — "Formatting your fields" · how-to · builder · 3194w · 0 inbound
    - YAML-based field formatting: hiding fields (`hidden: true`), grouping fields in the sidebar via `group_details` + `groups` (max 2 nesting levels), custom `description`/`label` overrides, `format` strings for rounding/compacting/currency units, and parameter-driven conditional formatting (`${ld.parameters.x}` substitution and ternary `${ld.parameters.x=="v"?"a":"b"}` expressions in format strings) — plus a shorter UI-side section on formatting custom metrics and table calculations (percent/currency/number with round/separator/compact/prefix/suffix).
    - ⚠ overlaps: guides/table-calculations — the "Formatting table calculations" section here duplicates the percent/currency/number formatting-option descriptions also given in guides/table-calculations' format section
  - **guides/period-over-period** — "How to do period-over-period analysis in Lightdash" · how-to · mixed · 3028w · 0 inbound
    - Big Value chart "Compare to previous row" for month-over-month and same-month-prior-year comparisons; trend-line comparisons by week-of-year or day-of-month with running totals; year-over-year via a `lag(x, 12)` table calculation; and a parameter-driven approach requiring a dbt/YAML `date_range` parameter plus an `additional_dimensions` SQL case statement to bucket rows into current/previous/previous-year periods (with a custom start/end date variant).
    - ⚠ overlaps: guides/formula-table-calculations and guides/table-calculations/sql-templates/percent-change-from-previous — same percent-change/period-comparison SQL patterns appear here as raw ad-hoc snippets, independently of the reusable formula function and SQL-template versions elsewhere
  - **guides/custom-fields** — "Using custom fields" · how-to · explorer · 1378w · 7 inbound
    - Creating and deleting custom metrics (from a dimension's options menu, or cloned from an existing YAML aggregate metric with filters/formatting editable and a read-only source-metric link) and custom dimensions (bins: fixed number, fixed width, custom range; and custom SQL dimensions, which are gated to admin/developer or a custom role with the "Manage Custom Fields" scope).
  - **guides/custom-tooltip** — "Custom tooltips" · how-to · explorer · 249w · 1 inbound
    - Configuring custom tooltips via Configure chart > Display > Custom Tooltip using HTML (JS disabled) and `${field}` variable interpolation from any chart axis, with a tip to add a hidden table calculation as an extra axis to surface data not otherwise plotted.
  - **guides/version-history** — "Version history" · mixed(how-to+reference) · explorer · 583w · 3 inbound
    - Restoring prior chart versions individually, or restoring a dashboard version (which also restores every chart saved *within* that dashboard, based on the dashboard version's timestamp, not per-chart granularity — so rolling back one dashboard change can silently revert an unrelated later chart edit); recently-deleted recovery (30-day window, self-hosted `SOFT_DELETE_ENABLED`/`SOFT_DELETE_RETENTION_DAYS`); and version-history retention by Cloud tier (Pro 30 days, Enterprise 60 days).
  - **guides/how-to-promote-content** — "How to promote content" · how-to · builder · 574w · 4 inbound
    - Promoting charts, dashboards (including nested data-app tiles and non-chart tiles), and data apps from a development project to an upstream project — matching is by name at first creation (survives renames), requires developer role and access to both projects/spaces, and auto-creates missing spaces in the upstream project.
  - **guides/verified-content** — "Verified charts and dashboards" · mixed(explanation+reference) · admin · 1660w · 3 inbound
    - Explains the verification model (admin/developer or custom-role `manage:ContentVerification` can verify; badge auto-removed if anyone but the verifier edits the content, with a Save/"Save & verify" confirmation dialog on edit), where verified content surfaces (homepage, listings, AI-agent content search and dashboard-tile previews, MCP `list_verified_content`), and the one-way bridge from saved+verified AI-agent answers into verified content; then documents the content-as-code YAML fields (`verification` read-only audit metadata vs. `verified` tri-state boolean: true/false/absent, with silent no-op skip on upload if the uploader lacks verify permission).
    - ⚠ overlaps: guides/ai-agents/verified-answers (outside this batch) — the "verified answers become verified content" bridge is described from this page's side; worth checking that page doesn't restate the same mechanics
  - **guides/instance-health-best-practices** — "Instance health best practices" · explanation · admin · 761w · 0 inbound
    - Best-practice framework across four pillars an Enterprise instance-health review grades against — Adoption (90-day query activity, >40% weekly-to-monthly stickiness, tracked by group), Access governance (group-based access, small admin set, no-expiry PATs via `PAT_MAX_EXPIRATION_TIME_IN_DAYS`, offboarding review), Content lifecycle (zero-view content, space-to-group alignment, verified canonical content), and Cost and performance (p95 latency, error rate, per-explore warehouse cost) — each with a recommended review cadence.
  - **guides/keyboard-shortcuts** — "Keyboard shortcuts" · reference · explorer · 180w · 0 inbound
    - Two shortcut tables — one global (Cmd/Ctrl+K for search) and one for the Query-from-tables/chart-editing context (run query, clear explore, copy table cell).
    - 🚩 flags: stub — only 3 shortcuts total documented across the whole app; likely missing shortcuts for dashboards, saved-chart editing, navigation, etc.
  - **Table calculations**
    - **guides/table-calculations** — "Table calculations" · how-to · explorer · 1350w · 3 inbound
      - Overview of table calculations as a computation layer on top of dimensions/metrics/custom fields/other table calcs; creating them via quick calculations, the custom SQL modal, or built-in row/pivot/aggregate functions; per-calculation Totals mode (Formula vs. Sum of rows vs. None); and the execution-order limitation that filters on a table calculation only hide rows after all calculations (including grand totals) have already run.
      - ⚠ overlaps: guides/formatting-your-fields — duplicate percent/currency/number formatting-option descriptions (see that page's overlap note)
    - **guides/formula-table-calculations** — "Formula table calculations" · reference · explorer · 1166w · 2 inbound
      - Spreadsheet-style formula syntax for table calculations (the default input mode, works on all 9 supported warehouses) — full function reference tables (math, logical, string, date, aggregation, window functions like `RUNNING_TOTAL`/`RANK`/`LAG`/`LEAD`/`MOVING_AVG`, null handling) plus `PARTITION BY`/`ORDER BY` window-clause syntax and worked examples (gross margin %, VIP flagging, running total, period-over-period growth %, customer bucketing).
      - ⚠ overlaps: guides/table-calculations/sql-templates and its subpages (running-total, rank-in-column, percent-change-from-previous, percent-of-total-column, rolling-window) — same calculation patterns (running totals, ranking, percent change, percent of total, rolling windows) are taught twice: once as formula functions here, once as raw copy-paste SQL there, with no cross-linking between the two except a shared "Related" link back to the overview
    - **Functions**
      - **references/table-calculation-functions/row-functions** — "Row functions" · reference · builder · 659w · 4 inbound
        - Documents 6 row functions (row, offset, index, lookup, offset_list, list) with syntax, parameter tables, and compiled-SQL accordions (ROW_NUMBER, LAG/LEAD, NTH_VALUE, CASE+MAX, ARRAY); notes results depend on the query's current sort order.
        - ⚠ overlaps: guides/table-calculations/table-calculation-sql-templates/running-total, .../rolling-window, .../rank-in-column, .../percent-change-from-previous, .../percent-of-previous-value — these copy-paste SQL guides hand-write the same LAG/window logic (previous-value diffs, rolling windows, running totals) that offset/offset_list now do as built-in functions, with no cross-links between the two systems; guides/formula-table-calculations — its "Window" function category (per inventory headings) likely covers the same offset/lookback concepts in spreadsheet-formula syntax
      - **references/table-calculation-functions/pivot-functions** — "Pivot functions" · reference · builder · 900w · 3 inbound
        - Documents 6 pivot functions (pivot_column, pivot_offset, pivot_index, pivot_where, pivot_row, pivot_offset_list) for referencing values across pivoted columns, each with compiled-SQL accordions showing adjacency-guarded LAG/LEAD/CASE logic; only available when a dimension is pivoted.
        - ⚠ overlaps: guides/table-calculations/table-calculation-sql-templates/percent-of-group-pivot-total — manual SQL template computing the same group/pivot-relative percentages that pivot_index/pivot_where could compute natively
      - **references/table-calculation-functions/aggregate-functions** — "Aggregate functions" · reference · builder · 423w · 1 inbound
        - Documents total() and row_total(), explaining they compile to separate CTEs (not window functions) so total() correctly re-runs a metric's native aggregation (handles count_distinct/average correctly, unlike naive summing); both only accept metric references, not dimensions.
        - ⚠ overlaps: guides/table-calculations/table-calculation-sql-templates/percent-of-total-column, .../percent-of-group-pivot-total — same "percent of total" calculation shown as raw copy-paste SQL rather than the native total()/row_total() functions; references/chart-types/table — "Incorrect totals" section's manual SUM()OVER() workaround is exactly the pattern total() replaces (and fixes for count_distinct); guides/formula-table-calculations — its "Aggregation" function category likely duplicates total()/row_total() in formula syntax
    - **SQL templates**
      - **guides/table-calculations/sql-templates** — "SQL templates" · reference · explorer · 102w · 2 inbound
        - Card-grid landing page linking to 7 copy-paste SQL template pages (percent change from previous, percent of previous value, percent of column total, percent of group/pivot total, rank in column, running total, rolling window); tips readers toward the built-in row/pivot functions as a simpler alternative to hand-written window SQL.
        - ⚠ overlaps: guides/formula-table-calculations — see that page's overlap note
      - **guides/table-calculations/table-calculation-sql-templates/percent-change-from-previous** — "Percent change from previous" · reference · explorer · 233w · 1 inbound
        - Copy-paste `(value / LAG(value) OVER (ORDER BY ...)) - 1` template for percent change vs. the previous row, with a reminder to set format to `percent`.
        - ⚠ overlaps: guides/formula-table-calculations — same pattern as its "Period-over-period growth %" example; guides/period-over-period — same percent-change concept expressed as an ad-hoc `lag()` snippet there
      - **guides/table-calculations/table-calculation-sql-templates/percent-of-previous-value** — "Percent of previous value" · reference · explorer · 227w · 1 inbound
        - Copy-paste `value / LAG(value) OVER (ORDER BY ...)` template for percent-of-previous-row, with a reminder to set the table calc's format to `percent`.
      - **guides/table-calculations/table-calculation-sql-templates/percent-of-total-column** — "Percent of column total" · reference · explorer · 137w · 1 inbound
        - Copy-paste `value / SUM(value) OVER()` template for percent-of-column-total, with a reminder to set format to `percent`.
        - ⚠ overlaps: guides/formula-table-calculations — same as its "Percent of total" formula example (`orders_revenue / SUM(orders_revenue) * 100`)
      - **guides/table-calculations/table-calculation-sql-templates/percent-of-group-pivot-total** — "Percent of group/pivot total" · reference · explorer · 177w · 2 inbound
        - Copy-paste `value / SUM(value) OVER (PARTITION BY group_column)` template for percent-of-group/pivot-total, with a reminder to set format to `percent`.
      - **guides/table-calculations/table-calculation-sql-templates/rank-in-column** — "Rank in column" · reference · explorer · 155w · 1 inbound
        - Copy-paste `ROW_NUMBER() OVER (ORDER BY ... DESC)` template for ranking a column, with ASC/DESC guidance for which end gets rank 1.
        - ⚠ overlaps: guides/formula-table-calculations — same pattern as the formula `RANK(PARTITION BY ..., ORDER BY ... DESC)` example
      - **guides/table-calculations/table-calculation-sql-templates/running-total** — "Running total" · reference · explorer · 280w · 1 inbound
        - Copy-paste `SUM(x) OVER (ORDER BY ... ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` template, with an ASC/DESC ordering tip.
        - ⚠ overlaps: guides/formula-table-calculations — same pattern as its `RUNNING_TOTAL()` function
      - **guides/table-calculations/table-calculation-sql-templates/rolling-window** — "Rolling window" · reference · explorer · 227w · 1 inbound
        - Copy-paste `SUM(x) OVER (ORDER BY ... ROWS BETWEEN N PRECEDING AND CURRENT ROW)` template (optionally as a ratio of two rolling sums, e.g. rolling average) for lookback-window calculations.
        - ⚠ overlaps: guides/formula-table-calculations — same pattern as its `MOVING_SUM`/`MOVING_AVG` functions

### Build with AI

- **guides/ai-overview** — "Using AI with Lightdash" · explanation · ai-builder · 798w · 1 inbound
  - Catalog of four AI surfaces (AI agents, Lightdash Data MCP, Lightdash Docs MCP, agent skills) with a comparison table of use case/requirements, MCP setup snippets for Cursor/Claude Code/Windsurf, and `lightdash install-skills` quick-start commands.
  - ⚠ overlaps: guides/choosing-ai-workflow — both maintain a table comparing AI agents vs. MCP vs. YAML/agent-skills and cross-link to each other as "the" decision guide.
  - 🚩 flags: naming — sidebarTitle "Overview" duplicates the sidebarTitle used by guides/ai-agents (a different hub one level down), which can be disorienting in the nav tree.
- **guides/choosing-ai-workflow** — "Choosing the right AI workflow in Lightdash" · explanation · ai-builder · 1351w · 1 inbound
  - Decision tree and detailed comparison table (workflow/best-for/typical-user/output/governance) across 9 AI workflows — in-app agents, Slack agents, Data MCP, Docs MCP, agent skills, content editing, dashboards as code, AI writeback, and reviews/evaluations — plus "important cautions" (MCP is power-user-only, broad agents underperform, don't upload sensitive knowledge docs).
  - ⚠ overlaps: guides/ai-overview — see above.
- **Data apps**
  - **guides/data-apps** — "Data apps" · mixed(how-to+reference) · ai-builder · 5266w · 8 inbound
    - The full data-apps feature guide: creating an app from 4 templates (Dashboard/Slide show/PDF report/Custom), attaching context (charts/dashboards/images/screenshots/sample data/live-linked charts with filter-propagation rules), the Sonnet/Opus/Haiku model picker, version restore and app duplication, the network inspector (Queries/Requests tabs, Inspect data lineage highlighting), CSV/XLSX/PDF export wiring, sharing via project role × space access, promoting apps out of preview projects, and the full apps-as-code CLI workflow (`lightdash download --apps-only`, on-disk layout under `lightdash/apps/<slug>/`, `lightdash upload --apps`, moving apps between projects/instances with `--create-new`).
    - ⚠ overlaps: guides/data-apps/external-connections — the "External connections" section's opening paragraph is reproduced near-verbatim from that page's intro. guides/ai-agents (hub) — none.
  - **guides/data-apps/external-connections** — "External connections" · reference · admin · 675w · 1 inbound
    - Documents external-connection config fields (name, base URL host-pinning, allowed methods defaulting to GET-only, allowed path prefixes), the 4 auth types (None, API key header/query, Bearer, Google service-account JSON exchanged for a short-lived token), the connection-test step with "Save as sample," and the freeform per-connection Instructions field (up to 10,000 chars) passed into the build sandbox.
    - ⚠ overlaps: guides/data-apps — see above.
  - **guides/data-apps/themes** — "Themes" · how-to · ai-builder · 628w · 1 inbound
    - Documents theme contents (CSS, fonts, images, `skill.md`, and an inline "Extra instructions" text field), the 100MB aggregate / 10MB per-file size caps, setting an org default theme, and applying a theme when creating a new app.
  - **guides/data-apps/visualizations** — "Data app visualizations" · how-to · ai-builder · 748w · 3 inbound
    - Documents generating a reusable single-tile chart renderer via the "Data app visualization" template, the declared typed field schema (dimension/metric/series), the interactive test panel for mapping fields to an explore before saving, and using a saved visualization as a chart type in the Explorer with field-mapping pickers.
  - **guides/data-apps/self-hosting** — "Self-hosting" · reference · platform · 898w · 4 inbound
    - Enterprise self-hosting config for the E2B sandbox + Claude provider: required env vars (`APPS_RUNTIME_ENABLED`, `APPS_S3_BUCKET`, `E2B_API_KEY`, `E2B_TEMPLATE_NAME`), Anthropic-vs-Bedrock provider options (`AI_DEFAULT_PROVIDER`, `BEDROCK_REGION`, `BEDROCK_API_KEY` or IAM key pair), the optional `E2B_TEMPLATE_TAG` pin, cost model (E2B per-sandbox-second, Claude per-token), the `view/create/manage:DataApp` scopes, and a troubleshooting table for sandbox/Anthropic/Bedrock build failures.
- **Lightdash AI agents**
  - **guides/ai-agents** — "AI agents" · explanation · explorer · 491w · 15 inbound
    - Landing/hub page for the AI agents feature area; a card grid linking to all 12 sub-pages plus a "Key features" bullet list (natural language queries, AI Router, memory, Reviews, knowledge documents, evaluations, deep research, AI writeback, content editing) and a Loom walkthrough embed.
    - 🚩 flags: naming — sidebarTitle "Overview" collides with guides/ai-overview's sidebarTitle at the parent nav level (see note there).
  - **guides/ai-agents/getting-started** — "Getting started with AI agents" · how-to · admin · 1750w · 14 inbound
    - Step-by-step setup: enabling AI features org-wide (Settings → Ask AI → General), setting an org default model with model-resolution order (user choice → agent default → org default → system default), creating an agent (name, instructions with 4 content components, knowledge docs, Data Access + Pass user information, User/Group Access, dbt tag-based field access with full YAML examples, AI writeback toggle), and Slack integration including single-agent vs. multi-agent channel setup with routing.
    - ⚠ overlaps: guides/ai-agents/data-access — the dbt tagging YAML code examples (tagging `status`/`location`/`amount` fields with ai/sales/operations/finance tags) are reproduced verbatim in both pages.
  - **guides/ai-agents/using-ai-agents** — "Using AI agents" · how-to · explorer · 2281w · 5 inbound
    - Covers core chat capabilities, SQL mode (off by default, gated by `manage:SqlRunner` ability, per-query human-in-the-loop approval, requires `aiRequireOAuth` in Slack), pinning a chart/dashboard as context via "Ask AI Agent", `@`-mentioning charts/dashboard tiles, read-only thread conditions, scheduling a recurring delivery from a conversation, period-over-period comparison prompts and limitations, and a known-limitations list (no forecasting/custom stats, no custom viz or big-number charts, no cross-session memory).
  - **guides/ai-agents/deep-research** — "Deep research" · how-to · explorer · 1472w · 1 inbound
    - Beta, Enterprise-only long-running investigation mode; documents the 4 research-depth tiers by warehouse-query cap (Low 10, Medium 25, High 50, Extra High 100), the mermaid workflow, run states (queued/running/completed/partially completed/failed/cancelled), report structure (confidence levels, chart snapshots vs. live data), and the `create:AiDeepResearch` scope requirement.
  - **guides/ai-agents/visibility** — "Agent visibility" · explanation · admin · 658w · 5 inbound
    - Frames agent governance as three pillars (transparency/control/governance) mapped to specific features; documents the Threads tab and MCP activity tab (90-day retention) under Settings → Ask AI, and a comparison table contrasting agents vs. raw warehouse SQL access on auditability.
  - **guides/ai-agents/ai-router** — "AI Router" · how-to · admin · 736w · 6 inbound
    - Explains automatic question routing between ≥2 accessible agents, enabling it at Settings → Organization Settings → Ask AI → General (requires AI features on first), writing per-project routing instructions with `@agent` tagging, and the `route_agent` MCP tool that lets external AI clients auto-select an agent without manual `list_agents`/`set_agent`.
  - **guides/ai-agents/agent-memory** — "Agent memory (Beta)" · explanation · explorer · 1129w · 2 inbound
    - Describes fully-automatic, per-user memory distillation from idle threads (corrections, stated preferences, feedback notes), daily curation (merge/supersede/retire), inline citations with a read-only memory detail page, explicit exclusions (preview projects, external MCP content, point-in-time values, one-off requests), and a comparison table vs. project context (personal/automatic/no-review vs. project-wide/Reviews-proposed/human-reviewed).
    - ⚠ overlaps: guides/ai-agents/reviews — both explain the `lightdash.project_context.yml` mechanism; agent-memory's comparison table restates what project context is and how it's applied (writeback PR to the YAML file).
  - **guides/ai-agents/verified-answers** — "Verified Answers" · how-to · admin · 734w · 6 inbound
    - Documents verifying an AI-generated chart/dashboard via the checkmark icon, the one-way link where verifying an answer also marks its saved chart/dashboard as verified content (green badge, surfaced in `list_verified_content` MCP tool), the 6-verified-answer threshold before suggested questions appear, and that only Admin/Developer roles can verify.
  - **guides/ai-agents/evaluations** — "Evaluations" · how-to · admin · 826w · 6 inbound
    - Explains building evaluation suites from manual questions or Threads-tab conversations, the LLM-as-judge factuality scorer (subset/superset/exact-match/contradiction/unimportant-difference grading), guidance for writing "expected response" fields with 6 concrete question/expected-response examples, and using thumbs-down feedback to seed eval sets.
  - **guides/ai-agents/reviews** — "AI agent reviews" · reference · admin · 1382w · 4 inbound
    - Beta Enterprise/Cloud Pro feature that scans agent turns for likely-wrong answers and proposes semantic-layer PRs or project-context entries; documents the full `lightdash.project_context.yml` schema (version, entries[].kind/content/id/terms/objects), how the writeback merges a single entry via the GitHub API preserving comments/order, and a decision table for semantic-layer vs. project-context fixes.
    - ⚠ overlaps: guides/ai-agents/agent-memory — see above.
  - **guides/ai-agents/data-access** — "Data access control" · reference · builder · 2018w · 14 inbound
    - Documents the two data-access modes (full vs. metadata-only), the "Pass user information" personalization toggle (name/role/group), and detailed dbt-tag-based field/explore filtering rules with 5 worked scenarios (explore-level-only, field-level-only, mixed, base+joined mixed, joined-only-hidden) plus how `sql_filter` and `required_attributes` user-attribute security automatically apply to agent-generated queries.
    - ⚠ overlaps: guides/ai-agents/getting-started — verbatim duplicate tagging YAML examples (see note there).
  - **guides/ai-agents/best-practices** — "Best practices for AI agents" · explanation · builder · 2701w · 5 inbound
    - Guidance on scoping agents narrowly (works best with AI Router), the 4 AI-hint levels (model/group/dimension/metric, string-or-array YAML syntax) with precedence over descriptions, knowledge-document mechanics (on-demand vs. "Always include in context" retrieval, 20KB/5MB quotas, per-agent scoping, edit/view/download from the fullscreen viewer) with what-to-upload/not-to-upload lists, and instruction-writing dos/don'ts with a good/bad example pair.
    - ⚠ overlaps: guides/ai-agents/verified-answers, guides/ai-agents/evaluations — the "[Using verified answers]" and "[Using evaluations]" sections are condensed bullet restatements of those two dedicated pages, presented as literal link-headers.
  - **guides/ai-agents/ai-writeback** — "AI writeback (Beta)" · how-to · builder · 2355w · 7 inbound
    - Beta feature letting chat trigger the `editDbtProject` tool to open a PR/MR against a GitHub/GitLab-backed dbt project; covers prerequisites (GitHub App or GitLab App install, Developer role), `@`-file mentions (GitHub-only), the PR review card (Preview/Diff/Merge with pinned-commit-SHA safety), impact analysis and "numbers still hold" verification before claiming a change safe, automatic PR preview projects, iterating on an existing open PR vs. opening a new one, and a fallback-behavior table for 7 failure conditions.
  - **guides/ai-agents/content-tools** — "Creating and editing content (Beta)" · how-to · admin · 614w · 4 inbound
    - Beta feature letting agents create/edit saved charts and dashboards when three conditions hold (data access on, content-editing toggle on, requesting user has dashboards-as-code permission); documents that turning off data access auto-disables and locks content editing, space-scoped save targets, and the Slack OAuth requirement for per-user permission evaluation.
  - **guides/ai-agents/mcp-servers** — "Connecting MCP servers" · how-to · admin · 1492w · 4 inbound
    - Project-level connection of external MCP servers (Notion, Linear, Confluence, Lightdash Docs, etc.) to agents; documents the 3 auth types (None/Bearer/OAuth), personal-vs-shared OAuth credential scopes and fallback order, per-tool permission curation, and example agent+MCP pairings (product analytics agent + Linear, support agent + Notion, RevOps agent + Confluence).
  - **guides/ai-agents/autopilot** — "Autopilot" · reference · admin · 613w · 3 inbound
    - Enterprise/Cloud Pro scheduled agent with a fixed per-run checklist (soft-delete 0-view charts >30 days, auto-fix charts referencing missing fields, flag slow (>2s) queries, flag stale previews >3 months, draft chart suggestions from repeated unanswered questions, surface unpinned popular content); documents action-type tags, safety rules (30-day floor, never deletes a dashboard's only chart, self-reverses mistakes), and the 3 permission toggles (read/create/modify).
  - **guides/ai-agents/agents-as-code** — "AI agents as code" · reference · builder · 1710w · 3 inbound
    - CLI-driven YAML serialization of agents under `lightdash/ai-agents/<slug>.yml`, opt-in via `--include-agents`/`--agents` on download and idempotent create-or-update by slug on upload; documents the full field reference (contentType, agentVersion 1|2, modelConfig, enableDataAccess/enableSelfImprovement/enableContentTools/enableUserContext), the `evaluations[]` suite sync-by-title semantics (no deletion of undeclared suites), what's excluded from YAML (threads, eval run history, access lists, Slack bindings, MCP refs, uploaded documents), and the two underlying API endpoints.
- **references/integrations/lightdash-mcp** — "Lightdash MCP" · mixed(reference+how-to) · app-dev · 5135w · 11 inbound
  - Comprehensive MCP doc covering client setup (Claude.ai/Desktop, ChatGPT "coming soon", OpenAI Codex, Claude Code CLI, Cursor), the full tool catalog (project/data exploration, run_metric_query/run_sql with permission gating, content-as-code create/edit tools gated by an admin kill-switch plus the create:ContentAsCode scope, create_scheduled_delivery gated by create:ScheduledDeliveries), project-pinning via the X-Lightdash-Project header, per-request row-level-security override via X-Lightdash-User-Attributes (org-admin only), agent-context bridging (route_agent/list_agents/set_agent), built-in skills exposed as MCP resources, and the separate free/public Docs MCP endpoint.
  - ⚠ overlaps: references/integrations/snowflake-cortex — MCP/Docs MCP setup re-explained there for Cortex specifically; guides/ai-agents/mcp-servers (outside batch, opposite direction — that page is about agents calling OUT to external MCP servers, this page is about assistants calling IN to Lightdash's MCP server) — not duplicated content but the near-identical naming ("MCP servers" vs "Lightdash MCP") is a discoverability trap.
  - 🚩 flags: naming — this exact page (references/integrations/lightdash-mcp) is placed in the nav under BOTH "Build with AI" and "Integrations" simultaneously per the inventory. It genuinely serves both audiences (AI-capability overview and integration reference), so dual nav placement is defensible, but it means any future edit must be checked against two different surrounding contexts, and it's the only page in either section duplicated this way.
- **Agentic development**
  - **guides/developer/agent-skills** — "Agent skills" · how-to · ai-builder · 575w · 6 inbound
    - Explains installing Lightdash's AI-coding-agent skills via the CLI (`lightdash install-skills --global|--agent cursor|codex|--path`), where they land (`.claude/skills/`, `.cursor/skills/`, `.codex/skills/`, GitHub Copilot reuses `.claude/skills/`), the alternate `npx skills add lightdash/lightdash@developing-in-lightdash` install path, and checking staleness via `lightdash --version`.
    - ⚠ overlaps: get-started/develop-in-lightdash/install-skills — both explain the same `lightdash install-skills` command, global-vs-project install locations, and prerequisites; near-duplicate content served from two different nav tabs (Build with AI > Agentic development vs. Build your semantic layer > Developer quickstart)
  - **guides/developer/editing-dashboards-with-agents** — "Editing dashboards with agents" · how-to · ai-builder · 485w · 4 inbound
    - Prescribes the download→edit-with-agent→upload→discard (`rm -rf lightdash/`) workflow specifically for AI coding agents, contrasted with when to commit dashboard code instead (templates, CI/CD, strict review); lists agent setup steps (install skills, connect Lightdash MCP, install/auth the CLI) and example natural-language prompts.
    - ⚠ overlaps: guides/developer/dashboards-as-code — "Disposable editing (recommended)" there restates this page's workflow and rationale nearly word-for-word; one of these two pages is redundant
  - **guides/developer/ai-writeback-self-hosting** — "AI writeback self-hosting" · reference · platform · 972w · 0 inbound
    - Self-hosting config for the AI writeback agent — env vars (`E2B_API_KEY`, `E2B_AI_WRITEBACK_TEMPLATE_NAME`, `AI_WRITEBACK_ANTHROPIC_API_KEY`, optional `E2B_AI_WRITEBACK_TEMPLATE_TAG`), supported dbt versions in the sandbox (1.8–1.11, clamping below 1.8), per-provider cost model (E2B per sandbox-second, Anthropic per token), Developer-role permission gate, and a troubleshooting list keyed to specific failure symptoms.

### Build your semantic layer

- **guides/lightdash-semantic-layer** — "Lightdash semantic layer" · explanation · builder · 1484w · 1 inbound
  - Conceptual essay on what a semantic layer is and how Lightdash's works — the three core components (metrics, dimensions, tables), the four-step build process (warehouse connection, YAML config, metadata enrichment, dynamic query generation), signs you're ready to adopt one, five ways to use it (UI, AI chat, agentic writeback, Python SDK, API), and benefits/challenges by audience.
  - ⚠ overlaps: guides/ai-overview — "AI-powered analytics" section restates the AI capabilities overview; guides/lightdash-yaml — both cover the dbt-vs-YAML decision
- **guides/lightdash-yaml** — "Lightdash YAML" · mixed(explanation+tutorial) · builder · 1058w · 3 inbound
  - Defines Lightdash YAML (define a semantic layer directly in YAML without a dbt project), gives a decision table for dbt vs. Lightdash YAML, and walks through a 5-step setup (create `lightdash.config.yml`, write a model YAML with metrics/dimensions, `lightdash lint`, `lightdash deploy --create --no-warehouse-credentials`, redeploy), plus an AI-agent-first path using `lightdash install-skills`.
  - ⚠ overlaps: guides/cli/how-to-use-lightdash-lint — validation step duplicates the lint guide; guides/cli/how-to-use-lightdash-deploy — deploy step duplicates the deploy guide; get-started/develop-in-lightdash/install-skills — install-skills instructions repeated
- **Developer quickstart**
  - **get-started/develop-in-lightdash/intro** — "Developing in Lightdash" · reference · builder · 217w · 1 inbound
    - Section landing page for the developer quickstart — a 10-item CardGroup (install CLI, authenticate, install skills, add tables, create dimensions/metrics, autogenerate YAML, preview, deploy, CI/CD), a note recommending a `lightdash` dbt tag convention, an embedded workflow video, and a link out to a separately-maintained Notion copy of the same quickstart.
  - **guides/cli/how-to-install-the-lightdash-cli** — "How to install the Lightdash CLI" · how-to · builder · 1551w · 12 inbound
    - Covers install methods (Homebrew, npm, WSL for Windows — recommended, native PowerShell, direct GitHub binary), extensive Windows troubleshooting (corporate TLS/Zscaler CA fix via `NODE_EXTRA_CA_CERTS`, PATH issues, non-interactive agent-terminal OAuth prompts failing inside Cursor/VS Code, blocked OAuth callback, "No active project" after login), and full update-to-latest/update-to-specific-version instructions for both Homebrew and npm.
    - ⚠ overlaps: guides/cli/how-to-upgrade-cli — near-total content duplication of the update instructions; references/lightdash-cli — `--version` section repeats the same npm/brew upgrade commands; guides/cli/cli-authentication — token-login workaround repeated in Windows troubleshooting
    - 🚩 flags: naming — title promises "install" but Windows troubleshooting and CLI-updating content (a separate concern, and a separate page elsewhere) make up over half the page
  - **guides/cli/cli-authentication** — "Authenticating your CLI" · how-to · builder · 486w · 5 inbound
    - Covers `lightdash login` browser OAuth flow, flexible instance-URL formats, silent re-authentication, token-based login for SSO users, the `LIGHTDASH_API_KEY`/`LIGHTDASH_URL`/`LIGHTDASH_PROXY_AUTHORIZATION` env vars for CI, and `lightdash config set-project` / `LIGHTDASH_PROJECT` for setting the active project.
    - ⚠ overlaps: guides/cli/how-to-install-the-lightdash-cli — Windows troubleshooting section repeats the token-login-without-OAuth workaround; guides/cli/automate-cli-workflow — `LIGHTDASH_API_KEY` secret setup duplicated
  - **get-started/develop-in-lightdash/install-skills** — "Install Lightdash Skills" · how-to · ai-builder · 527w · 3 inbound
    - Explains `lightdash install-skills` (defaults to Claude Code), what the skills enable (build-from-scratch via warehouse profiling, bulk chart/table renames, tool migrations, best-practice review, iterative conversational development), and the `/developing-in-lightdash` prompt-prefix convention; a tip notes skills are verified-content-aware but won't auto-prefer verified content unless told to.
    - ⚠ overlaps: guides/developer/agent-skills — condensed teaser of that page's "Installing skills" section (same install command, points there for other agents/custom paths)
  - **get-started/develop-in-lightdash/adding-tables-to-lightdash** — "Adding and managing Tables" · tutorial · builder · 1518w · 7 inbound
    - Adding/managing Tables via CLI: `lightdash dbt run -s my_model` generates the table + schema.yml, `lightdash preview` to test, `lightdash deploy` (with `--target prod` guidance for dev-profile setups) to ship; the three Table-visibility modes (entire project / by dbt tag / explicit model list); tagging models for visibility with dbt v1.9/v1.10+/Lightdash-YAML tab variants; and advanced tips (one YAML file per model, dbt selection syntax, generate-all-in-one-command).
    - ⚠ overlaps: get-started/setup-lightdash/get-project-lightdash-ready — near-identical "What are Tables?" section and the same dbt-tag/selector code examples | guides/cli/how-to-auto-generate-schema-files — same `lightdash generate`/`dbt run -s` schema-generation content
  - **get-started/develop-in-lightdash/how-to-create-dimensions** — "How to create dimensions" · tutorial · builder · 1307w · 3 inbound
    - Creating dimensions: definition (a documented dbt column), generating via CLI (`lightdash generate -s` / `lightdash dbt run -s`) vs. manual YAML edits (dbt v1.9/v1.10+/Lightdash-YAML tab variants), previewing with `lightdash preview`, configuring `meta.dimension` properties (label, description, etc.), and deploying via CLI (`git checkout main && git pull && lightdash deploy`) or the UI's "refresh dbt" button.
    - ⚠ overlaps: get-started/develop-in-lightdash/how-to-create-metrics — identical structure and near-identical wording for the CLI-vs-manual and deploy sections, swapping "dimension" for "metric" | get-started/setup-lightdash/get-project-lightdash-ready — repeats the same intro framing pattern
  - **get-started/develop-in-lightdash/how-to-create-metrics** — "How to create metrics" · tutorial · builder · 1129w · 6 inbound
    - Creating metrics: definition (calculations on dimensions) with a full `count_distinct`/`sum` YAML example across dbt v1.9/v1.10+/Lightdash-YAML tabs, two embedded Loom videos (column-level vs. table-level metrics), adding a metric to a model, previewing, configuring `meta.metrics` properties (label, groups, round), deploying to production, and a pointer to on-the-fly custom metrics in the Explore view.
    - ⚠ overlaps: get-started/develop-in-lightdash/how-to-create-dimensions — near-verbatim shared preview/deploy boilerplate
  - **guides/cli/how-to-auto-generate-schema-files** — "Autogenerate Lightdash-ready YAML files for your models" · how-to · builder · 620w · 3 inbound
    - Explains `lightdash generate` / `lightdash dbt run` to auto-create and sync `schema.yml` from warehouse columns, dbt selection syntax for scoping generation (`-s tag:prod`, `+model+`), and how dbt doc blocks get automatically inserted into generated YAML for consistent field descriptions.
    - ⚠ overlaps: get-started/develop-in-lightdash/adding-tables-to-lightdash — likely covers the same `generate` workflow as part of the Tables tutorial
  - **guides/cli/how-to-use-lightdash-preview** — "Preview local changes" · how-to · builder · 348w · 8 inbound
    - Short guide distinguishing `lightdash preview` (blocking, deletes on keypress) from `lightdash start-preview` (detached, needs `lightdash stop-preview`), the same strict-compilation flags as the deploy/validate pages, hot-refreshing via `dbt compile` in a separate terminal (no auto hot-reload — manual browser refresh required), and a caution about local-only warehouse credentials not being reachable from the Lightdash instance.
    - ⚠ overlaps: guides/developer/preview-projects — a much fuller preview-projects guide (creation via CLI/App/PRs, permissions, expiration) that largely subsumes this CLI-specific page; guides/cli/how-to-use-lightdash-deploy and how-to-use-lightdash-validate — identical strict-compilation flag text
  - **guides/cli/how-to-use-lightdash-deploy** — "Deploy changes to production" · how-to · builder · 599w · 7 inbound
    - Explains `lightdash deploy` (pushes local dbt profile + uncommitted project state to production; warns it can overwrite prod if your default dbt target points elsewhere), the `--profile` flag for multi-target setups, the `--no-partial-compilation` / `--validate-warehouse-columns` strict-compilation flags, and the safer `lightdash refresh` alternative (or UI "Refresh dbt" button) that re-syncs metadata using Lightdash-stored credentials instead of the local profile.
    - ⚠ overlaps: guides/cli/how-to-use-lightdash-preview and guides/cli/how-to-use-lightdash-validate — identical strict-compilation flag explanations repeated near-verbatim in all three pages
  - **guides/cli/automate-cli-workflow** — "Automate with CI/CD" · how-to · builder · 1899w · 2 inbound
    - Sets up CI/CD (GitHub Actions) for the CLI: storing `LIGHTDASH_API_KEY`/`LIGHTDASH_PROJECT`/`LIGHTDASH_URL` and a per-warehouse `DBT_PROFILES` template as GitHub secrets, then copying `start-preview.yml`/`close-preview.yml` (PR previews) and `deploy.yml` (production deploy) workflows, plus three strategies for per-developer dev profiles (profile targets, GitHub environments, dbt Cloud CI schema).
    - ⚠ overlaps: guides/cli/how-to-use-lightdash-deploy — links back here for CI/CD framing; references/lightdash-cli — same commands referenced without flag-level detail
- **Developer guides**
  - **guides/cli/how-to-use-lightdash-lint** — "How to use lightdash lint" · how-to · builder · 751w · 1 inbound
    - Documents `lightdash lint` for validating model/chart/dashboard YAML against JSON schemas: CLI/verbose/SARIF (`--format json`) output modes, error-vs-warning severity and exit codes, the three schema types and how they're auto-detected, and how it integrates with AI coding tools (Lightdash Skills auto-run it) and GitHub Actions/Code Scanning.
    - ⚠ overlaps: get-started/develop-in-lightdash/install-skills — "install skills first" messaging repeated; guides/lightdash-yaml — validation step overlaps
  - **guides/developer/dbt-model-best-practices** — "dbt model best practices for Lightdash" · explanation · builder · 1381w · 0 inbound
    - Frames wide-materialized-table vs. multi-join-model modeling as a performance/self-serve/analytical-depth tradeoff with a comparison table and named patterns (wide-for-self-serve, both-pathways, multi-join-only); recommends one `.yml` file per dbt model; and gives a warehouse-cost/performance strategy table (materialize as table, partition/cluster by warehouse, pre-aggregates, caching, limiting exposed models via dbt tags, monitoring via query tags).
  - **guides/developer/preview-projects** — "Preview projects" · explanation · builder · 1879w · 8 inbound
    - Explains preview-project behavior across its three creation surfaces (CLI `lightdash preview`/`start-preview`, the Lightdash app's "+ Create Preview" incl. GitHub-branch previews, and GitHub Actions PR automation) — permission inheritance (only content the creator can access is copied), where connection settings come from per surface (local `profiles.yml`, copied from parent project, or the GitHub Action's `DBT_PROFILES` secret), and preview expiration rules (project admin sets default/max hours, `--expires-in` clamps to max, re-running `start-preview` refreshes or resets `expires_at`).
    - ⚠ overlaps: guides/cli/how-to-use-lightdash-preview (outside this batch) — 8+ anchor-level cross-references into that page's sections (`#dbt_profiles`, developer-credentials setup, etc.) suggest heavy structural overlap between this conceptual guide and that CLI-specific one; worth checking whether they duplicate the same ground from two angles
  - **guides/developer/dbt-write-back** — "dbt write-back" · how-to · builder · 1136w · 8 inbound
    - Covers three write-back paths — Custom Metrics (requires a base dimension; cloned metrics can't write back) and Custom Dimensions (bin dimensions unsupported) from the Explorer sidebar's three-dot menu, and dbt models from SQL Runner (lands in `models/lightdash/` tagged `created-by-lightdash`) — plus the Table Configuration setting changes needed to surface a new model as a Table, and running it via manual `dbt run` or a dbt Cloud job triggered on PR.
  - **guides/developer/pull-requests** — "Pull requests" · reference · builder · 758w · 1 inbound
    - Documents the project-settings "Pull requests" table aggregating every PR/MR Lightdash has opened (from custom-metric/custom-dimension write-back, SQL runner, source editor, or an AI agent) — live-fetched state/title even if the PR was merged/closed outside Lightdash, and the opt-in "Connect GitHub account" flow (currently feature-flag gated) that reattributes future write-back PRs to the user's own GitHub identity instead of the Lightdash bot.
  - **guides/developer/dashboards-as-code** — "Dashboards as code" · mixed(how-to+reference) · builder · 7183w · 11 inbound
    - Covers choosing between disposable (download-edit-upload-discard) vs. Git-managed dashboard workflows; full `lightdash download`/`upload` flag reference (`-c`/`-d`/`-p`/`--project`/`--strip-pivot-series`/`--include-virtual-views`/`--include-charts`); the `.lightdash-metadata.json` change-tracking file; space management via `spaceSlug` and access-aware `.space.yml` (with `inheritParentPermissions`, `projectMemberAccessRole`, per-user/per-group roles, three on-disk layouts); virtual views and custom roles as code (full YAML contracts, exit codes, `--force` semantics); the complete dashboard/chart/SQL-chart YAML schema; and VS Code JSON-schema validation setup.
    - ⚠ overlaps: guides/developer/editing-dashboards-with-agents — the "Disposable editing (recommended)" section duplicates that page's entire download→edit→upload→discard workflow and rationale bullets almost verbatim | guides/developer/vs-code-yaml-validation — the "JSON schema validation in VSCode" section repeats the same install-extension + `yaml.schemas` settings.json steps, just pointing at chart/dashboard-as-code schemas instead of the dbt model schema | guides/developer/virtual-views — covers the same feature from the YAML/CLI angle rather than the UI angle, worth cross-linking more directly
    - 🚩 flags: none — correctly the canonical home for this topic, but it's a 7,000+ word kitchen-sink page bundling at least five loosely-related subsystems (download/upload workflow, spaces/permissions, virtual views, custom roles, IDE setup) that could be split for navigability
  - **guides/developer/sql-runner** — "SQL runner" · how-to · builder · 453w · 6 inbound
    - Developer/admin-only ad-hoc SQL workspace with four panels (warehouse table list, schema, query builder, results) and a query-history drawer; after running a query you can save it as a chart (aggregation types: count/any/sum/average/max/min), turn it into a virtual view, write it back to dbt, or share a draft link; flags that `ORDER BY` isn't guaranteed because results are wrapped in a subquery to support dashboard filtering.
  - **guides/developer/virtual-views** — "Virtual views" · how-to · builder · 150w · 3 inbound
    - Minimal guide to creating a Virtual View from the SQL Runner's save dropdown (it then appears in Tables for anyone to query), editing/deleting it via the Explorer's three-dot menu (changes propagate to all dependent content), and a pointer to dbt write-back instead if the query will be reused long-term.
    - ⚠ overlaps: guides/developer/dashboards-as-code — that page's "Virtual views" section documents the same feature in much greater depth (full YAML contract, CLI flags, validation errors, destructive-change `--force` behavior) but from the code angle; this page doesn't link to it
    - 🚩 flags: stub — 150 words for a first-class content type that gets a full YAML/CLI reference elsewhere; this UI-only page reads as a placeholder next to that depth
  - **guides/cli/how-to-use-lightdash-validate** — "Lightdash Validate" · how-to · builder · 600w · 2 inbound
    - Documents `lightdash validate` for catching broken content before merge: default vs. `--project <uuid>` vs. `--preview` scope, `--only tables charts dashboards` filtering, the same `--no-partial-compilation` / `--validate-warehouse-columns` strict flags (noting tables are validated by default and drive exit status), and wiring `start-preview` + `validate --preview` into GitHub Actions to block merges on errors.
    - ⚠ overlaps: guides/developer/validating-your-content — covers the same concept from the app UI side rather than CLI; how-to-use-lightdash-deploy/preview — same strict-compilation flag text repeated a third and fourth time
  - **guides/developer/validating-your-content** — "Validating your content" · explanation · builder · 556w · 1 inbound
    - Explains what triggers the content validator (Refresh dbt, `lightdash deploy`, project-settings "Test and compile", manual "Run validation," `lightdash validate`), what it catches (renamed/deleted metric or dimension references in tables/charts/dashboards) vs. what it doesn't (runtime SQL errors like invalid syntax or divide-by-zero), how to fix errors (UI rename, `lightdash rename`, or dashboards-as-code find/replace), and that dismissing an error is global (affects every user) and reappears on the next run if unresolved.
    - ⚠ overlaps: guides/cli/how-to-use-lightdash-validate (outside this batch) — both describe when/how the validator runs, one conceptually and one as CLI-command reference; likely redundant on the "when validation runs" list specifically
  - **guides/developer/renaming-models-and-fields** — "Renaming models, metrics, and dimensions" · how-to · builder · 713w · 1 inbound
    - Covers admin-only renaming of models/metrics/dimensions project-wide — via the validation page's per-chart "Fix" / "Fix all occurrences" flow, or the CLI (`lightdash rename --type field --from --to --model --dry-run --list`, lowercase+underscore names only) — with a worked CLI example (rename in dbt → preview → validate → rename in preview → merge → rename in prod → re-download code-managed content), and reverting via chart version history.
  - **guides/developer/using-parameters** — "How to use parameters" · reference · builder · 3182w · 5 inbound
    - Exhaustive parameter reference — string/number/date types defined in `lightdash.config.yml` (project-level) or model `meta.parameters` (model-level, incl. `options_from_dimension` and labeled options); referenced via `${lightdash.parameters.x}` or the `ld` alias; usable in dimension/metric/table/join/table-calculation/SQL-Runner SQL with worked examples for each, including a "metric-swap" pattern (one parameterized metric standing in for several KPIs); type-cast workarounds via `::type`; Liquid `{% raw %}{% case/if %}{% endraw %}` blocks needed to swap tables/schemas in `sql_from` since parameters always render as quoted string literals; the built-in `date_zoom` reserved parameter (shadowable by a same-named user parameter); and per-chart/per-dashboard saved parameter values.
  - **guides/developer/caching** — "Caching" · explanation · builder · 1070w · 3 inbound
    - Documents two cache types (filter-value caching, always-on; chart/dashboard results caching, opt-in via Lightdash support) as an instance-wide (not per-project) toggle, the cache key (project ID + generated SQL), which features read/write the cache (saved charts, dashboard tiles, scheduled deliveries, Google Sheets syncs, SQL runner, Metrics Catalog) vs. which bypass it (chart edit mode), rolling 24h expiry, and how time-precision in filters affects cache hit rate.
    - ⚠ overlaps: guides/developer/dbt-model-best-practices — that page's "Leverage caching" section restates this page's cache-performance framing (brief summary + link, not a real duplicate)
    - 🚩 flags: misplaced — this is an instance-wide, support-ticket-gated feature governed at the account/warehouse-cost level, closer in spirit to admin/instance-health content than the CLI- and dbt-workflow-centric pages surrounding it in "Developer guides"
  - **guides/developer/timezones** — "Working with timezones in Lightdash" · explanation · builder · 3374w · 2 inbound
    - Two-part guide — Setup: pick timezone-aware warehouse column types over naive ones, use `DATE` only for calendar values, set the connection's default data timezone for naive timestamps, set the project timezone, opt columns out of conversion (`convert_timezone: false`) or override `timestamp_domain` (`aware`/`naive`) when the warehouse catalog is wrong. Daily use: per-chart timezone resolution priority (embed URL param > project/user/specific setting), how relative vs. absolute date filters and cell-click filters behave, day-vs-hour grouping under DST (25h/23h days, doubled/missing hourly bars), profile-timezone scope, dashboard/scheduled-delivery timezone independence, embedded-dashboard resolution, and a troubleshooting accordion plus a worked one-row/four-configuration example table.
  - **guides/developer/explores** — "Creating tailored tables from a single dbt model" · how-to · builder · 944w · 0 inbound
    - Documents the `explores` meta config for defining multiple curated table variants from one dbt model — each with its own `label`, `joins`, exposed `fields`, and `required_attributes` gating (e.g. an exec-only view) — plus explore-scoped `additional_dimensions` for joined-table dimensions that shouldn't live at the base-model level.
    - 🚩 flags: naming — page title ("Creating tailored tables from a single dbt model"), sidebar label ("Multiple tables from one model"), and URL slug (`explores`) are three different names for the same concept; someone searching for "explores" from the config keyword won't recognize the title or sidebar entry
  - **guides/developer/filtering-dashboard-in-url** — "How to filter a dashboard in the URL" · how-to · builder · 294w · 0 inbound
    - Shows how to build a dashboard URL with a `?filters=` URL-encoded JSON param, then generalize it by swapping the literal filter value for `${ value.formatted | url_encode }` inside a dbt model's YAML `sql` so clicking a dimension value elsewhere links to a pre-filtered dashboard.
    - 🚩 flags: stub — written instructions are a 4-line numbered list; the actual walkthrough is carried almost entirely by two embedded Loom videos rather than text
  - **guides/developer/vs-code-yaml-validation** — "How to set up Lightdash YAML validation for VS Code" · how-to · builder · 126w · 0 inbound
    - Adds a `yaml.schemas` entry (Lightdash's `lightdash-dbt-2.0.json` schema, matched against `/**/models/**/*.yml`) to VS Code `settings.json`, plus the `redhat.vscode-yaml` extension, so dbt model YAML gets inline validation against the same schema Lightdash uses at compile time.
    - ⚠ overlaps: guides/developer/dashboards-as-code — that page's "JSON schema validation in VSCode" section is the same two-step setup (install extension, add `yaml.schemas` block) pointed at a different schema (chart/dashboard-as-code instead of dbt model); candidate to merge into one "YAML schema validation in VS Code" page covering both schema URLs
    - 🚩 flags: stub — 126 words, no description in frontmatter
  - **guides/developer/cli/how-to-compile-your-lightdash-project** — "Test changes with Lightdash compile" · how-to · builder · 1274w · 0 inbound
    - Sets up a GitHub Action that runs `lightdash compile` on every PR; covers partial vs. strict compilation (`--no-partial-compilation`, `--validate-warehouse-columns`, noting `PARTIAL_COMPILATION_ENABLED=false` is no longer supported), adding GitHub secrets (`LIGHTDASH_API_KEY`, `LIGHTDASH_PROJECT`, `LIGHTDASH_URL`, `DBT_PROFILES` with per-warehouse YAML templates for BigQuery/Postgres/Redshift/Snowflake/Databricks), and copying the workflow file from the `lightdash/cli-actions` repo.
    - ⚠ overlaps: guides/cli/automate-cli-workflow (outside this batch) — both walk through the identical GitHub-secrets setup (same four secret names, same per-warehouse `DBT_PROFILES` templates) and workflow-file creation steps, just wired to a different action (compile vs. preview/deploy); strong candidate for consolidation
    - 🚩 flags: naming — lives at `guides/developer/cli/...`, a one-off subfolder that duplicates the sibling top-level `guides/cli/` folder where every other CLI how-to (preview, deploy, lint, validate, automate-cli-workflow) actually lives; nothing else in the "Developer guides" nav group is nested this way
  - **guides/cli/how-to-upgrade-cli** — "Upgrading your Lightdash CLI" · how-to · builder · 67w · 0 inbound
    - Two-sentence stub: compare your CLI version (`lightdash --version`) to the version shown in the Lightdash UI footer, then run `npm install -g @lightdash/cli` to upgrade. Doesn't mention Homebrew even though Homebrew is the primary recommended install path elsewhere.
    - ⚠ overlaps: guides/cli/how-to-install-the-lightdash-cli#updating-the-lightdash-cli — same upgrade instructions covered more completely (includes Homebrew and version-pinning); references/lightdash-cli#version — same npm command shown a third time
    - 🚩 flags: stub — 60 words, fully redundant with a fuller section on how-to-install-the-lightdash-cli; also oddly placed in nav (see systemic notes)
- **Semantic layer reference**
  - **references/metrics** — "Metrics reference" · reference · builder · 10681w · 12 inbound
    - Full property/type reference for defining metrics in dbt/Lightdash YAML — 17 metric types (percentile, median, average, boolean, count, count_distinct, date, max, min, number, sum, sum_distinct, average_distinct, string, percent_of_previous, percent_of_total, running_total) grouped into aggregate/non-aggregate/post-calculation categories, plus config properties (format, compact, separator, groups, urls, richText, show_underlying_values, filters, tags) and a "level of detail" (LOD) workaround pattern using a pre-computed dbt CTE for coarser-grain denominators.
    - ⚠ overlaps: references/dimensions — Format/Compact/Separator sections use byte-for-byte identical tables and near-identical prose to the metrics version, duplicated rather than shared via a snippet; guides/formatting-your-fields — re-explains format/compact/hidden/groups/rounding as a task guide, a third copy of the same formatting reference content; references/pre-aggregates/overview — "Supported metric types"/non-reaggregatable metric list overlaps conceptually with metric categories here (not literally duplicated, but two separate canonical lists of which metric types behave how)
  - **references/dimensions** — "Dimensions reference" · reference · builder · 10929w · 14 inbound
    - Full property reference for dimensions — type, time_intervals (with full date/numeric/string granularity option tables and custom granularities via lightdash.config.yml), groups, urls, richText, required_attributes/any_attributes, convert_timezone, timestamp_domain, color, case_sensitive, filter_autocomplete, image display (WIP), tags, and additional_dimensions (derived/virtual dimensions for formatting, comparisons, JSON parsing, multi-timezone columns).
    - ⚠ overlaps: references/metrics — identical Format/Compact/Separator tables copy-pasted; guides/formatting-your-fields — third copy of the same format/compact/groups/hidden content as a walkthrough; references/lightdash-config-yml — "Using custom granularities" here duplicates definition mechanics also explained in lightdash-config-yml's Custom granularities configuration section; guides/developer/timezones — convert_timezone/timestamp_domain get a short treatment here and a much longer, overlapping treatment there; references/tables — both pages have a "Case sensitive" section explaining the same three-level (dimension > table > project) precedence
  - **references/tables** — "Tables reference" · reference · builder · 5793w · 9 inbound
    - Property reference for Table-level YAML config — order_fields_by, groups/table_groups, sql_from, sql_filter (row-level security via user attributes), primary_key (single/composite), default_show_underlying_values, default_filters/required_filters with a full filter-operator table, case_sensitive, model-level parameters (with options_from_dimension), explores (multiple named views of one model), and sets (reusable field-list references with expand `*` and exclude `-field` operators).
    - ⚠ overlaps: guides/developer/using-parameters — the parameter property table (label, type, options, default, multiple, allow_custom_values, options_from_dimension) is reproduced near-verbatim here and again in lightdash-config-yml, a three-way duplicate of the same schema; guides/filters / guides/limiting-data-using-filters — the default_filters operator table overlaps with the general filters reference (is, contains, between, inThePast, etc.); references/joins — the "Sets" section's join-field-referencing example depends on joins.mdx content
  - **references/joins** — "Joins reference" · reference · builder · 7249w · 5 inbound
    - Full join YAML reference — join/alias/label/type (inner/left/right/full)/relationship (one-to-many, many-to-one, one-to-one, many-to-many)/always/fields/hidden — plus an extensive, worked-example-heavy explanation of SQL fanouts (which metric types are fanout-safe, single vs. chained vs. multiple one-to-many join scenarios, how Lightdash's cte_keys/cte_metrics/cte_unaffected CTE pattern deduplicates, and named limitations: joined-dimension references, filtered dimensions from joins, multi-level references, rolledup metrics, intentional fanouts) and a "joins are not transitive" section with three resolution strategies (materialize in dbt, chain the join, exclude via fields).
    - ⚠ overlaps: guides/developer/dbt-model-best-practices — the opening Info callout's "prefer wide tables over BI-layer joins" guidance duplicates that guide's core recommendation; references/tables — "Sets" and "Explores" sections both depend on/repeat join mechanics
  - **references/writing-descriptions** — "Writing useful descriptions in your semantic layer" · explanation · builder · 1281w · 1 inbound
    - Best-practices essay (not a property reference) on writing model/dimension/metric descriptions — a "what a good description answers" checklist (grain, source, values/formula, alternatives, transformations, gotchas) with before/after examples, plus a second half on layering `ai_hint` YAML metadata on top of human-facing `description` at model/dimension/metric level, with guidance on when to use which.
    - ⚠ overlaps: guides/ai-agents/best-practices — that guide's "Document your data thoroughly" / "Using AI hints" sections cover the same ai_hint concept and decision criteria (when to use ai_hint vs description) as this page's back half, effectively duplicating the same teaching in two different nav locations (Semantic layer reference vs. Build with AI guides)
    - 🚩 flags: naming — this page reads as an explanation/style-guide, unlike its reference-doc siblings (metrics/dimensions/tables/joins are all property references); its type doesn't match the "reference" pattern of the section it lives in, though the nav placement itself is defensible
  - **Pre-aggregates**
    - **references/pre-aggregates/overview** — "Pre-aggregates" · mixed(explanation+reference) · builder · 1896w · 3 inbound
      - Conceptual introduction to pre-aggregates (Enterprise Beta) — the define→materialize→match→serve lifecycle with a worked daily-to-monthly re-aggregation example, query-matching rules (field coverage, grain reachability, filter compatibility), a "supported metric types" reference (sum/count/min/max/average only), an extensive "current limitations" reference (personal warehouse connections, parameters, user attributes in SQL, custom metrics/dimensions, SQL table calculations), and a full comparison table against results caching (configuration, trigger, storage, coverage, scope) with guidance on using both together.
      - ⚠ overlaps: guides/developer/caching — that guide's own "Results caching vs pre-aggregates" section is a second, independently-written comparison of the same two systems, covering largely the same ground (when to use which, that they're complementary layers) as this page's "Pre-aggregates vs results caching" section; references/pre-aggregates/getting-started — the "Filtered pre-aggregates" concept and its `order_date: inThePast 52 weeks` example is explained almost identically on both pages (this page under Query matching, getting-started under its own "Filtered pre-aggregates" heading)
    - **references/pre-aggregates/getting-started** — "Getting started with pre-aggregates" · reference · builder · 1489w · 2 inbound
      - Configuration reference for the `pre_aggregates` YAML block — full property table (name, dimensions, metrics, filters, time_dimension+granularity pairing, max_rows, refresh.cron, materialization_role), a repeated filtered-pre-aggregate example (52-week rolling window) with query-compatibility rules, multiple-pre-aggregates-per-model guidance (prefer several small ones over one wide one), refresh scheduling via cron (with a materialization-triggers table: compile/cron/manual), max_rows truncation warning, materialization_role for pinning a stable user-attribute context on joined/access-controlled models, and a complete end-to-end example with joins.
      - ⚠ overlaps: references/pre-aggregates/overview — duplicates the "Filtered pre-aggregates" concept/example almost word-for-word; references/tables — materialization_role's use case (required_attributes/any_attributes on joined tables) depends on and restates access-control concepts from Tables reference
    - **references/pre-aggregates/monitoring** — "Monitoring and debugging pre-aggregates" · reference · builder · 752w · 4 inbound
      - Documents the Project Settings > Pre-aggregates UI — Materializations tab (Active/In progress/Failed lifecycle states, one active materialization per pre-aggregate), Analytics tab (hit/miss stats breakable by explore, query context, chart/dashboard), and a full "why a query misses a pre-aggregate" table mapping 12 miss reasons (no pre-aggregates defined, dimension/metric/filter-dimension not covered, non-additive metric, custom SQL metric, granularity too fine, custom dimension/metric present, table calculation present, user bypass) to the YAML fix for each, plus a per-dashboard "Pre-aggregation audit" / "Rebuild pre-aggregates" menu.
      - ⚠ overlaps: references/pre-aggregates/cli-audit — the miss-reason table here is the canonical source that cli-audit.mdx explicitly points back to rather than re-deriving (good practice, not duplicative)
    - **references/pre-aggregates/cli-audit** — "Auditing pre-aggregates from the CLI" · how-to · builder · 877w · 3 inbound
      - Task-oriented guide to `lightdash pre-aggregate-audit` — auditing one dashboard by slug/UUID vs. `--all`, reading the hit/miss/ineligible tile output format, project resolution order (--project flag > LIGHTDASH_PROJECT_UUID > saved CLI config), `--json` output for scripting (with a jq example filtering dashboards with misses), an iteration loop (audit → edit YAML → deploy → refresh → re-audit), CI gating via `--fail-on-miss` with a GitHub Actions snippet, and an explicit callout that this command's structured miss output pairs well with agentic coding tools (Cursor/Claude Code/Codex) for closing coverage gaps autonomously.
      - ⚠ overlaps: references/lightdash-cli — the `lightdash pre-aggregate-audit` command is also listed (briefly, with a pointer here) in the CLI reference's command table, which is the correct reference→detail split, not duplicative
  - **references/lightdash-cli** — "Lightdash CLI reference" · reference · builder · 9112w · 13 inbound
    - Exhaustive command/flag reference for the `lightdash` CLI — login, config set/get/rename-project, compile (partial vs. strict compilation, --validate-warehouse-columns), preview/start-preview/stop-preview, deploy vs. refresh (with a comparison table), validate (including --include-spaces/--exclude-spaces), generate/dbt run, download/upload (with the full data-apps, AI-agents, virtual-views, custom-roles flag surface), rename, diagnostics, sql, warehouse-catalog, lint, and pre-aggregate-audit — plus shared dbt options (node selection, flags, --defer caveats, vars, threads) and CI/CD environment variables.
    - ⚠ overlaps: guides/cli/* (how-to-install-the-lightdash-cli, cli-authentication, how-to-use-lightdash-preview, how-to-use-lightdash-deploy, how-to-use-lightdash-validate, how-to-auto-generate-schema-files, automate-cli-workflow, how-to-use-lightdash-lint, how-to-upgrade-cli) — this reference page is the flag-by-flag catalog while guides/cli/* are task-oriented walkthroughs for the same commands; the split is structurally sound but specific content is duplicated rather than linked, e.g. the "deploy pushes to production" Warning callout also appears in guides/cli/how-to-use-lightdash-deploy, and the GitHub Actions CI/CD example here largely repeats guides/cli/automate-cli-workflow's own workflow YAML; guides/developer/dashboards-as-code — download/upload flag documentation (charts, dashboards, apps, agents, virtual-views, spaces) is exhaustively repeated in both places; guides/lightdash-yaml — does NOT duplicate CLI reference content itself, but its "Step 3/4/5" (validate/deploy/update) steps point to and lightly restate lightdash validate/deploy usage already fully covered here
  - **references/lightdash-config-yml** — "lightdash.config.yml reference" · reference · builder · 3103w · 8 inbound
    - Reference for the optional project-root `lightdash.config.yml` file — top-level sections `spotlight` (Metrics Catalog default_visibility + categories), `parameters` (project-level, with reserved name `date_zoom`), `defaults` (case_sensitive, column_totals, additional_time_intervals, granularity_labels with a documented override hierarchy), `custom_granularities` (fiscal_quarter-style custom date truncation SQL), and `table_groups` (sidebar group labels, up to 3 levels). Explicitly not supported for dbt-Cloud-connected projects.
    - ⚠ overlaps: guides/developer/using-parameters and references/tables — the parameter property table (label/type/options/default/multiple/allow_custom_values/options_from_dimension, including the "labeled options" sub-example) is reproduced near-verbatim in all three places; guides/metrics-catalog — the spotlight config section here duplicates that guide's own explanation of default_visibility/categories; references/dimensions — case_sensitive hierarchy and custom-granularities mechanics are explained on both pages; references/tables — table_groups configuration here duplicates the Groups section's explanation of the same feature
  - **references/sql-variables** — "SQL variables" · reference · builder · 167w · 1 inbound
    - Short enumeration of the SQL template variables available inside Lightdash YAML SQL expressions — `${field}`, `${model.field}`, `${TABLE}`, `${lightdash.attributes.*}` (with `ld`/`attr` aliases), and `${lightdash.user.email}` (with a verified-email requirement caveat and self-hosted SMTP/SSO pointer).
    - ⚠ overlaps: references/tables — sql_filter row-level-security examples use `${lightdash.attributes.*}` documented here; references/workspace/user-attributes — the user-attribute referencing syntax is defined here but used extensively there

### Workspace and user management

- **references/workspace/spaces** — "Spaces" · mixed(reference+how-to) · builder · 1135w · 2 inbound
  - Documents Spaces as chart/dashboard folders — moving/renaming/deleting items, Inherited vs Restricted access modes with root-vs-nested inheritance rules, the nested-space "breaking inheritance" copy-on-switch behavior, and best practices (prefer groups over individuals, plan hierarchy before nesting).
  - ⚠ overlaps: references/workspace/roles — the Space Roles table (Full Access/Can Edit/Can View) and its group-inheritance example is substantively re-described here in prose rather than just linked to the canonical table in roles.mdx.
  - 🚩 flags: misplaced — like pinning.mdx, this is an everyday builder-facing content-organization topic, filed among org-admin-only docs (SCIM, SSO, service accounts) in the same directory.
- **references/workspace/default-user-spaces** — "Default user spaces" · reference · admin · 393w · 0 inbound
  - "Default user spaces" (aka personal/personal user spaces) auto-provision a personal space per eligible user — Interactive Viewer+ or a custom role with Manage Saved Chart + Manage Explore — on their next login, named `Default User Spaces / <name>`, with the user set as admin of their own space.
  - ⚠ overlaps: references/workspace/spaces — conceptually adjacent (space admin/access model) but no duplicated instructions.
- **references/workspace/pinning** — "Pinning" · how-to · builder · 211w · 1 inbound
  - Explains pinning saved charts/dashboards/spaces so they appear as the first group of items on the project homepage; only users with editor or admin project access can pin/unpin via the three-dot menu.
  - ⚠ overlaps: none substantive.
  - 🚩 flags: misplaced — a project content-curation feature usable by any editor, not an org-governance topic, yet filed next to SCIM, custom roles, and groups.
- **references/workspace/personal-tokens** — "Personal access tokens" · how-to · builder · 47w · 4 inbound
  - Extremely thin page — one sentence explaining PATs authenticate the CLI/API, plus a single screenshot of the "generate token" button under project settings → personal access tokens. No rotation, expiry, revocation, or scoping information.
  - ⚠ overlaps: references/workspace/service-accounts — conceptually adjacent (both are token-based CLI/API auth) but complementary rather than duplicative — service-accounts.mdx explicitly frames itself as "a userless alternative to Personal Access Tokens."
  - 🚩 flags: stub — 6 lines total, no lifecycle management content at all, unlike the much more thorough service-accounts.mdx; misplaced — this is an individual/non-admin developer task (create your own token) filed among org-admin settings docs.
- **references/workspace/service-accounts** — "Service accounts" · mixed(reference+how-to) · admin · 1210w · 0 inbound
  - Enterprise machine-to-machine auth — org-scoped (single role across all projects) vs project-scoped (per-project role) service accounts; create/edit/rotate flows (`ldsvc_` token prefix, edits preserve the token); system-role and custom-role permission assignment; deprecated legacy `org:view`/`org:edit`/`org:admin` scopes; and CLI/API usage examples.
  - ⚠ overlaps: references/workspace/custom-roles — cross-linked for custom-role assignment. references/workspace/personal-tokens — same token-based CLI/API auth concept, explicitly framed as its machine-auth counterpart.
- **references/workspace/personal-warehouse-connections** — "Personal warehouse connections" · reference · explorer · 393w · 5 inbound
  - Explains that personal (per-user) warehouse credentials are only used on projects configured to "require user credentials" (otherwise the shared project connection is used), and documents supported auth methods per warehouse: Snowflake (OAuth/private key/password), Databricks (OAuth/PAT), BigQuery (OAuth/service account), Redshift (AWS IAM SSO/CLI creds/password), others via username+password.
  - ⚠ overlaps: references/organization-credentials — describes the same "require user credentials" override relationship from the opposite side; the two pages only cross-link one way.
  - 🚩 flags: misplaced — like personal-tokens.mdx, this is an individual end-user settings task ("My warehouse credentials" in user settings), not an admin/workspace-governance topic, yet sits among org-admin docs.
- **references/organization-credentials** — "Organization credentials" · mixed(how-to+reference) · admin · 846w · 0 inbound
  - Enterprise feature letting org admins create reusable warehouse credentials (currently Snowflake SSO only) that developers/project admins can select at project-creation time instead of entering raw connection details; covers create/edit/delete, preview-project inheritance, and how it interacts with a project's "require user credentials" setting.
  - ⚠ overlaps: references/workspace/personal-warehouse-connections — both describe the same "require user credentials" override relationship from opposite sides (org creds set up the project; personal creds override for querying), with only a one-directional cross-link between them. references/workspace/custom-roles — briefly mentions restricting the project-creation permission via custom roles.
  - 🚩 flags: misplaced — file lives directly under references/ while every sibling admin/org doc (roles, custom-roles, personal-warehouse-connections, etc.) lives under references/workspace/; outdated — numbered setup steps are out of order (1, 2, 4, 5, 6, then 8, 7), a likely CMS-export artifact.
- **Admin**
  - **references/admin** — "Lightdash Admin References" · reference · admin · 59w · 0 inbound
    - Bare landing page — a CardGroup with four links (usage analytics, roles and permissions, groups, user attributes) and one-line blurbs; no original content of its own.
    - 🚩 flags: stub — thin index page; naming — titled "Lightdash Admin References" implying an overview of "all admin functions and features," but links to only 4 of the 25+ workspace admin pages, so the title overpromises what the page actually indexes.
  - **references/workspace/roles** — "Roles and permissions" · reference · admin · 4742w · 21 inbound
    - The core roles/permissions reference — org-vs-project role relationship with a worked "Acme, Inc." example combining org roles, project roles, and groups; the three space roles (Full Access/Can Edit/Can View) with an inheritance walkthrough; allowed-email-domains auto-join config; default-project setting; and two full permission matrices (~70 project scopes, ~13 org scopes).
    - ⚠ overlaps: references/workspace/custom-roles — both explain org-vs-project role scoping. references/workspace/spaces — the Space Roles table and its group-inheritance example are re-described in prose in spaces.mdx rather than just linked to, a near-duplicate of the canonical table here. get-started/setup-lightdash/invite-new-users (outside batch) — the "Allowed email domains" section here reuses the same screenshots and instructions as that guide.
    - 🚩 flags: none structural, but a strong consolidation candidate given the space-roles and allowed-email-domains duplication noted above.
  - **references/workspace/custom-roles** — "Custom Roles" · mixed(reference+how-to) · admin · 3212w · 11 inbound
    - Enterprise custom roles — org-level vs project-level role types (immutable once chosen), the additive-only permission model (custom project roles can't subtract what an org role already grants), a detailed scope reference for the content-as-code ladder (view/create/manage:ContentAsCode plus `@self` variants), the four preview-edit `@self` scopes, three independent SQL-authoring scopes, and the custom-roles-as-code YAML workflow (`lightdash download/upload --organization`).
    - ⚠ overlaps: references/workspace/roles — both explain the org-vs-project role relationship and link to each other's permission matrices. guides/developer/dashboards-as-code#custom-roles-as-code (outside batch) — this page's "Managing custom roles as code" section is explicitly a preview of that page's fuller custom-roles-as-code workflow.
    - 🚩 flags: none — long (328 lines) but coherently a reference doc, appropriately placed.
  - **references/workspace/user-attributes** — "User attributes" · reference · builder · 2605w · 18 inbound
    - Defines user attributes (org-wide, admin-managed, text-only, multi-value-capable) and their four YAML surfaces — `sql_filter` row filtering, `required_attributes`/`any_attributes` column and table access control (AND vs OR logic, and how to combine both), `sql_on` join filtering — plus the `${lightdash.attributes.*}`/`${lightdash.user.email}` SQL variable syntax (email requires verification), chart/dashboard filter-value usage (explicitly not a security boundary, unlike `sql_filter`), current bypass limitations (SQL runner, custom SQL/subqueries in table calculations), and how multi-valued user+group attributes combine (union for column access, IN-list for row filtering).
    - ⚠ overlaps: references/sql-variables (outside batch) — likely shares documentation of the `${lightdash.attributes...}` / `${lightdash.user...}` SQL variable syntax; not confirmed without reading that page directly.
  - **references/workspace/user-impersonation** — "User impersonation" · reference · admin · 353w · 1 inbound
    - Feature-flagged capability for org admins to impersonate any user in their org (hard 15-minute session limit, audit-logged with both userUuids, banner-based start/stop from Settings → Users & Groups), contrasted with support-impersonation.mdx's vendor-support-side toggle; documents personal-warehouse-credential interaction during impersonated queries.
    - ⚠ overlaps: references/workspace/support-impersonation — substantial structural duplication (audit-logging description and personal-warehouse-credential warning are nearly identical wording across both pages).
    - 🚩 flags: stub — an unresolved `{/* TODO: Add image here */}` HTML comment is left in the source (line 38), an incomplete-content marker shipped in production docs.
  - **references/workspace/support-impersonation** — "Lightdash support access" · reference · admin · 281w · 0 inbound
    - Org-wide opt-in toggle (default off) letting the Lightdash support team impersonate users to reproduce support-ticket issues, distinct from self-service user-impersonation; documents audit-logging (both userUuids logged) and personal-warehouse-credential interaction during impersonated queries, with the single enable location (Settings → General).
    - ⚠ overlaps: references/workspace/user-impersonation — near-duplicate structure: both describe who can enable it, the same audit-logging behavior (both userUuids), and nearly identical personal-warehouse-credential-interaction wording — a shared "how impersonation works" section could replace both restating it independently.
    - 🚩 flags: naming — filename is `support-impersonation.mdx` but the page title is "Lightdash support access," a euphemism that obscures this is about impersonation by the vendor's support team (not org admins), unlike the similarly-named user-impersonation.mdx.
  - **references/workspace/managing-your-organization** — "Managing your organization" · how-to · admin · 334w · 0 inbound
    - Documents the Settings "Danger zone" — any member can leave an org from Settings → Profile (must promote another admin first if they're the sole admin); only org admins can permanently delete the entire org and all its content from Settings → General; both require typing the org name to confirm.
    - ⚠ overlaps: none substantive.
  - **references/workspace/export-limits** — "Export limits" · reference · admin · 452w · 1 inbound
    - Org-level override panel (gated by the `pro-limits` feature flag) for two instance defaults — max query rows (`LIGHTDASH_QUERY_MAX_LIMIT`) and max CSV/Excel cells (`LIGHTDASH_CSV_CELLS_LIMIT`) — with server-side ceilings and a per-surface enforcement table (explorer/SQL runner, CSV/Excel downloads, scheduled deliveries, API).
    - ⚠ overlaps: none substantive within batch.
  - **references/workspace/how-to-create-multiple-projects** — "Creating multiple projects" · how-to · admin · 220w · 1 inbound
    - Org-admin steps to create a new project (Organization settings → All projects → Create new), switch between projects via the top-corner dropdown, and delete projects individually or in bulk.
    - ⚠ overlaps: get-started/setup-lightdash/connect-project — explicitly deferred to for the actual connection-setup instructions, so no duplicated content, just delegation.
    - 🚩 flags: naming — filename/slug undersells the page, which also documents switching and deleting projects, not just creating multiple ones.
  - **references/workspace/customizing-the-appearance-of-your-project** — "Customizing the appearance of your project" · mixed(how-to+reference) · admin · 673w · 1 inbound
    - Org-level color-palette creation with separate light/dark-mode variants, and the 6-level palette-resolution hierarchy for chart rendering: embed/SDK > chart > dashboard > space (and parent spaces) > project > organization.
    - ⚠ overlaps: references/react-sdk#color-palettes (outside batch) — explicitly cross-referenced for the embed/SDK precedence level, likely duplicating part of the hierarchy explanation.
    - 🚩 flags: naming — sidebarTitle is "Color palettes" but the title/filename is "customizing-the-appearance-of-your-project," so slug, title, and sidebar label all diverge; outdated — frontmatter has `Description:` capitalized instead of `description:` (line 4), which likely breaks the meta description.
  - **references/workspace/adding-slack-integration** — "Add the Slack integration" · how-to · admin · 166w · 5 inbound
    - Org-admin-only steps to install the Lightdash Slack app (Organization settings → Integrations → Add to Slack), including the Slack-side approval flow, with a 2-minute walkthrough video.
    - ⚠ overlaps: none substantive — only forward-links to guides/using-slack-integration (usage, not setup) and the Slack integration reference, no duplicated instructions.
    - 🚩 flags: misplaced — a setup how-to filed under a references/ path alongside pure reference docs (roles, groups, etc.); naming — title "Add the Slack integration" vs slug "adding-slack-integration" (imperative vs gerund mismatch, minor).
  - **references/workspace/usage-analytics** — "Usage analytics" · reference · admin · 744w · 9 inbound
    - Two usage-tracking mechanisms — auto-generated per-project "Usage Analytics" dashboards (User Activity dashboard field glossary: weekly querying %, top queriers/editors, users stale for 90+ days, plus self-hosted `EXTENDED_USAGE_ANALYTICS` dashboard/chart view-count rankings) and query tags (metadata attached to warehouse queries, with a per-warehouse table of how each stores it — BigQuery labels, Snowflake/ClickHouse/Redshift/Databricks/Postgres/Athena SQL comments, Trino HTTP header).
    - ⚠ overlaps: none substantive.
  - **references/workspace/project-compilation-history** — "Project compilation history" · reference · admin · 365w · 1 inbound
    - Documents the compilation-history log (Project settings → Compilation history) — who/when/source (Refresh dbt, CLI Deploy) triggered each compile, per-entry detail (metric/dimension/explore counts, dbt models included, failed-explore error messages), and the in-progress log view shown during a UI "Refresh dbt."
    - ⚠ overlaps: none substantive.
  - **references/workspace/groups** — "Groups" · how-to · admin · 471w · 8 inbound
    - Org-admin group management (Cloud Pro/Enterprise; self-hosted needs `GROUPS_ENABLED`) — create/edit/delete groups, assign roles to groups via the Project access page, assign user attributes to groups, plus a deprecated OKTA-group-sync section that now points to SCIM.
    - ⚠ overlaps: references/workspace/scim-integration — the deprecated "Using OKTA to manage groups" section here is superseded by SCIM's "Group role provisioning" section, which covers the same OKTA-group-sync territory in more current detail. references/workspace/roles and references/workspace/user-attributes — cross-linked for project-role assignment and attribute assignment respectively.
    - 🚩 flags: outdated — the bottom third of the page ("Using OKTA to manage groups in Lightdash") is marked deprecated and redirects to SCIM; keeping deprecated setup instructions alongside current content is a maintenance smell.
  - **references/workspace/scim-integration** — "SCIM Integration" · mixed(how-to+reference) · admin · 2841w · 6 inbound
    - Enterprise SCIM 2.0 provisioning setup (`SCIM_ENABLED` env var, token generation, per-IdP walkthroughs for Okta/Azure/Google Workspace), SCIM behavior details (inactive-user deprovisioning, sole-admin protection, one-role-per-org/project), the standard `roles` attribute for org/project role provisioning with JSON examples, a deprecated Lightdash extension schema, group-role provisioning patterns, and token rotation via curl.
    - ⚠ overlaps: references/workspace/groups — this page's "Group role provisioning" section supersedes groups.mdx's deprecated OKTA-sync section. references/workspace/custom-roles — project role values reference custom roles by UUID.
    - 🚩 flags: none structural, though at 530 lines it mixes deep how-to (Okta/Azure click-paths), reference (role schema, RFC citations), and troubleshooting without a clear split — a candidate to split into a setup guide plus a role-provisioning reference.
  - **references/workspace/sso-providers** — "SSO providers" · reference · admin · 774w · 3 inbound
    - Plan-availability matrix for SSO providers (Google on all paid plans; Okta/Azure AD/OneLogin/Generic OIDC Enterprise-only), the Cloud-vs-self-hosted setup split, SSRF-protection URL-validation rules for the Okta-domain and OIDC-metadata-endpoint fields, and one-paragraph per-provider summaries linking to self-host setup guide anchors.
    - ⚠ overlaps: references/workspace/configure-sso — near-identical Cloud-vs-self-hosted framing and verified-domain prerequisite restated in both. references/workspace/verified-domains — cross-linked, with some routing-logic restated in both places.
    - 🚩 flags: naming — unclear scope separation from configure-sso.mdx (this page is "which provider is available," that one is "how to set one up," but both restate the same prerequisites and Cloud/self-hosted split) — a reader could land on either expecting the other's content.
  - **references/workspace/configure-sso** — "Configure SSO" · mixed(how-to+reference) · admin · 1058w · 2 inbound
    - Documents Lightdash Cloud's self-serve SSO panel (Organization settings → Single Sign-On) — required fields per provider (Okta, Azure AD, OneLogin, Generic OIDC, Google), redirect-URL table, email-domain routing logic, the lenient password-sign-in-hidden rule, per-organization-vs-instance-level precedence, and the two independent SSO account-linking toggles.
    - ⚠ overlaps: references/workspace/sso-providers — near-duplicate Cloud-vs-self-hosted framing and verified-domain prerequisite, both linking to the same self-host SSO page. references/workspace/verified-domains — restates the "Override organization domains" routing mechanic in similar terms. self-host SSO config page (outside batch) — this page repeatedly defers to it for the actual provider-side app-creation steps (Okta, Azure, OneLogin, Google), so the real walkthroughs live there, not here.
    - 🚩 flags: none structural, but see the SSO-routing overlap cluster noted under sso-providers and verified-domains.
  - **references/workspace/verified-domains** — "Verified domains" · reference · admin · 484w · 4 inbound
    - Domain-ownership verification via a one-time email passcode, used exclusively to control SSO routing (first-come-first-served per domain), explicitly distinguished from "allowed email domains" (which control auto-join instead); documents the "Override organization domains" per-SSO-method routing-subset mechanic.
    - ⚠ overlaps: references/workspace/configure-sso and references/workspace/sso-providers — the verified-domain prerequisite and "Override organization domains" mechanic is restated with overlapping but non-identical wording across all three pages, a 3-page cluster describing the same SSO-routing relationship.
    - 🚩 flags: none structural.
  - **references/workspace/custom-email-domain** — "Custom email domain" · how-to · admin · 488w · 0 inbound
    - Enterprise feature to send scheduled-delivery and alert emails from the org's own domain (DKIM + custom return-path for DMARC alignment); walks through entering the sending domain/from-address, adding DNS TXT/CNAME records, verifying, and toggling sending on/off, with a Cloudflare "must be DNS-only, not proxied" gotcha.
    - ⚠ overlaps: none substantive.
- **references/workspace/feature-maturity-levels** — "Feature Maturity Levels" · explanation · admin · 297w · 12 inbound
  - Defines the three-stage feature lifecycle (Experimental → Beta → GA) with a mermaid diagram and bullet lists of what each label promises about stability, breaking changes, support, and access restrictions.
  - ⚠ overlaps: none — a glossary concept that many feature pages link to (e.g., Beta labels on agent memory, AI writeback) but none duplicate its definitions.
  - 🚩 flags: misplaced — a general glossary/explanation page with zero admin-settings content, filed under references/workspace/ alongside org-settings docs; it's referenced from all over the product (any Beta/Experimental feature), so it reads more like a top-level concept page than a workspace-admin reference.

### Integrations

- **references/integrations** — "Lightdash Integration Reference Docs" · reference · builder · 53w · 0 inbound
  - Card-grid index for integration reference docs, but only links to 4 of the 7 pages that actually exist in references/integrations/ (dbt Projects, dbt MetricFlow, Slack, Google Sheets) — it omits lightdash-mcp, metrics-sql-api, and snowflake-cortex entirely.
  - 🚩 flags: stub — index is missing 3 of its 7 child pages, so those pages have no discovery path from the section landing page; outdated — card copy is truncated mid-sentence ("You can easily make changes in...", "Syncs") rather than real descriptions.
- **references/integrations/dbt-projects** — "dbt Projects" · reference · builder · 3347w · 7 inbound
  - Documents the five mechanisms for syncing a dbt project into Lightdash (local `lightdash deploy`, CI `lightdash deploy`, UI "Refresh dbt" button, CI `lightdash refresh`, direct `POST /refresh` API call), with a full decision matrix, recommended setups per team type, and a deep dive on dbt targets/profiles.yml and environment variables (including the blocked env-var-name list and schema-naming-by-target macro examples).
  - ⚠ overlaps: guides/cli/how-to-use-lightdash-deploy (outside batch) — the deploy mechanism is explained in both places; guides/cli/automate-cli-workflow (outside batch) — CI/CD setup duplicated conceptually.
- **references/integrations/dbt-metricflow** — "dbt MetricFlow metrics" · reference · builder · 991w · 1 inbound
  - Documents how `lightdash deploy` translates dbt MetricFlow semantic_models/metrics from the compiled manifest.json into Lightdash metrics — supported measure aggregation types, filter-template translation rules (same-model `Dimension()` references only), ratio/derived metric translation (same-model constraint explained), and an exhaustive "not supported yet" table (cumulative, conversion, cross-model ratios/derived, offset_window, percentile_type discrete).
- **references/integrations/slack-integration** — "Slack integration" · reference · admin · 926w · 8 inbound
  - Documents required OAuth scopes (links:read/write for unfurling), settings (notification channel for failed deliveries, bot avatar, AI Agents OAuth-requirement toggle for per-user attribution), private-channel setup, link unfurling behavior (uses the installer's warehouse credentials), scheduled delivery/alert delivery to Slack, and AI agent chat routing via @-mention in Slack.
  - ⚠ overlaps: references/workspace/adding-slack-integration (outside batch) — this page cross-links it for the install walkthrough, no content duplication; guides/using-slack-integration (outside batch) — that page is a 70-word stub that just links back here for the real content.
  - 🚩 flags: none for this page itself, but see systemic note on the Slack three-way split below.
- **references/integrations/google-sheets** — "Google Sheets" · how-to · explorer · 571w · 3 inbound
  - Documents creating a Google Sheets Sync from a saved chart's three-dot menu (name, frequency options from hourly to custom cron, tab selection/creation), plus the admin-facing "Syncs & Scheduled deliveries" project settings overview page for monitoring, editing, and deleting syncs.
  - ⚠ overlaps: none within batch; conceptually parallel to guides/how-to-create-scheduled-deliveries (outside batch) — both features share the same "Syncs & Scheduled deliveries" settings overview page but are documented as fully separate features.
- **references/integrations/lightdash-mcp** — "Lightdash MCP" · mixed(reference+how-to) · app-dev · 5135w · 11 inbound
  - Comprehensive MCP doc covering client setup (Claude.ai/Desktop, ChatGPT "coming soon", OpenAI Codex, Claude Code CLI, Cursor), the full tool catalog (project/data exploration, run_metric_query/run_sql with permission gating, content-as-code create/edit tools gated by an admin kill-switch plus the create:ContentAsCode scope, create_scheduled_delivery gated by create:ScheduledDeliveries), project-pinning via the X-Lightdash-Project header, per-request row-level-security override via X-Lightdash-User-Attributes (org-admin only), agent-context bridging (route_agent/list_agents/set_agent), built-in skills exposed as MCP resources, and the separate free/public Docs MCP endpoint.
  - ⚠ overlaps: references/integrations/snowflake-cortex — MCP/Docs MCP setup re-explained there for Cortex specifically; guides/ai-agents/mcp-servers (outside batch, opposite direction — that page is about agents calling OUT to external MCP servers, this page is about assistants calling IN to Lightdash's MCP server) — not duplicated content but the near-identical naming ("MCP servers" vs "Lightdash MCP") is a discoverability trap.
  - 🚩 flags: naming — this exact page (references/integrations/lightdash-mcp) is placed in the nav under BOTH "Build with AI" and "Integrations" simultaneously per the inventory. It genuinely serves both audiences (AI-capability overview and integration reference), so dual nav placement is defensible, but it means any future edit must be checked against two different surrounding contexts, and it's the only page in either section duplicated this way.
- **references/integrations/metrics-sql-api** — "Metrics SQL API" · reference · app-dev · 1009w · 1 inbound
  - Documents the Postgres-wire-protocol interface for querying the semantic layer from any Postgres client — connection fields, information_schema-based catalog discovery, query examples (including calculated expressions and a Python psycopg2 example), self-hosting via PGWIRE_PORT/TLS cert config, and hard limitations (no joins/subqueries/CTEs, no DML, simple query protocol only, no pg_catalog emulation).
- **references/integrations/snowflake-cortex** — "Snowflake Cortex" · how-to · ai-builder · 341w · 0 inbound
  - Documents three ways to pair Snowflake's Cortex Code agent with Lightdash — installing skills via `lightdash install-skills --agent cortex`, adding the Lightdash MCP via `cortex mcp add`, and adding the public Docs MCP — with a comparison table of auth requirements and where each works (locally vs. Snowsight Cortex).
  - ⚠ overlaps: references/integrations/lightdash-mcp — re-explains the same Lightdash MCP and Docs MCP setup/benefits, just with Cortex-specific install commands instead of Claude/Cursor/Codex ones; guides/developer/agent-skills (outside batch) — skills installation instructions overlap.
  - 🚩 flags: naming/misplaced — this reads as an agentic-development doc (parallel to guides/developer/editing-dashboards-with-agents and the "Build with AI > Agentic development" nav section) but lives under references/integrations instead; it's the only per-tool agentic-coding integration page, so it's an odd fit next to dbt/Slack/Google Sheets.

### Lightdash SDK

- **sdk/python-sdk** — "Python SDK" · reference · app-dev · 1422w · 1 inbound
  - Full reference for the Lightdash Python SDK (`pip install lightdash`): `Client` setup with instance_url/project_uuid/access_token, single-call vs. chainable query-builder patterns, dimension/metric attribute access with fuzzy matching and tab-completion, Python-operator filters (comparisons, `.in_()`, `.between()`, `&`/`|` combinators) with a supported-operators-by-type table, sorting, `ResultSet` conversions (pandas/polars `to_df`, `to_records`, `to_json_str`) with pagination, a raw `client.sql()` escape hatch, and the `LightdashError`/`QueryError`/`QueryTimeout`/`QueryCancelled` exception hierarchy.
  - ⚠ overlaps: guides/lightdash-semantic-layer — "Python client" section briefly restates this page's purpose; api-reference/v1/recipes — both solve "query Lightdash programmatically," one via curl/PAT, one via SDK

### Embedding

- **guides/embedding/how-to-embed-content** — "How to embed content" · how-to · app-dev · 1640w · 4 inbound
  - The embedding section's landing/quickstart page — describes all five embeddable content types with use cases, compares Shareable URL vs. iframe vs. React SDK in a feature matrix (including which methods support which content types and whether CORS is needed), CORS setup instructions, and a 4-step dashboard iframe quickstart with screenshots.
  - ⚠ overlaps: guides/embedding/dashboards (its Step 1-4 setup section duplicates this page's quickstart almost exactly); references/react-sdk (CORS setup instructions repeated).
  - 🚩 flags: none — functions correctly as the index/quickstart for the whole embedding cluster.
- **Embedding guides**
  - **guides/embedding/dashboards** — "How to Embed Dashboards" · how-to · app-dev · 2058w · 6 inbound
    - The most complete embed guide — dashboard filter interactivity (enabled: all/some/none, hidden, canAddFilters), parameter interactivity, export options (CSV/images/PDF), date zoom, explore-from-here plus writeActions for saving charts, view-underlying-data, view-data-apps (canViewDataApps), then both iframe (URL structure, theming) and React SDK (props, programmatic filters) implementation paths side by side with a full JWT example.
    - ⚠ overlaps: references/embedding (dashboard token and every interactivity option duplicated near-verbatim); references/iframe-embedding (iframe URL/theming section duplicated); references/react-sdk (Dashboard component props duplicated); guides/embedding/how-to-embed-content (quickstart Steps 1-4 duplicated).
    - 🚩 flags: none — reference content is intentionally mirrored here for a single-page walkthrough experience.
  - **guides/embedding/charts** — "How to Embed Charts" · how-to · app-dev · 1113w · 5 inbound
    - Step-by-step chart-embedding guide — React-SDK-only (no iframe support), a comparison table versus dashboard embedding, feature configuration (canExportCsv, canExportImages, canViewUnderlyingData), explicit limitations (read-only, no filters, no explore access), and KPI-widget/marketing-page use-case examples.
    - ⚠ overlaps: references/react-sdk (Chart component props repeated); references/embedding (chart token repeated) — intentional guide/reference split.
  - **guides/embedding/ai-agents** — "How to embed AI agents" · how-to · app-dev · 931w · 3 inbound
    - Step-by-step guide to embedding an AI agent via the React SDK's `Lightdash.AiAgent` — resuming threads via threadUuid/onThreadChange, the iframe URL path for direct thread resumption (`/embed/{projectUuid}/ai-agents/{agentUuid}/threads/{threadUuid}#{jwt}`), required token fields (content.type, agentUuid, writeActions), and access-control scoping rules (a token for agent A cannot reach agent B; agent reads/writes are confined to writeActions.spaceUuid).
    - ⚠ overlaps: references/react-sdk (AiAgent component props repeated); references/embedding (aiAgent token structure repeated) — intentional guide/reference split.
  - **guides/embedding/metrics-catalog** — "How to embed the metrics catalog" · how-to · app-dev · 840w · 3 inbound
    - Step-by-step guide to embedding the metrics catalog via `Lightdash.MetricsCatalog` — prerequisites, token generation with the canExplore flag and writeActions claim, access-control rules (catalog scoped to one project, tokens rejected across embed types), and a read-only-catalog example that simply omits canExplore.
    - ⚠ overlaps: references/react-sdk (MetricsCatalog component props repeated); references/embedding (metricsCatalog token repeated) — intentional guide/reference split.
  - **guides/embedding/data-apps** — "How to embed data apps" · how-to · app-dev · 681w · 3 inbound
    - Step-by-step guide for standalone data app embedding (iframe-only, no React SDK component exists for it) — enabling the app in the embed allowlist, minting a dataApp JWT scoped to one appUuid, constructing the `/embed/{projectUuid}/app/{appUuid}` URL, and rendering the iframe; explains the project-wide warehouse-column-access tradeoff the token grants and compares standalone embedding to dashboard-tile data apps.
    - ⚠ overlaps: references/iframe-embedding (URL pattern duplicated); references/embedding (dataApp token duplicated); guides/embedding/dashboards#view-data-apps (the tile-based alternative, cross-referenced rather than duplicated).
- **Reference**
  - **references/embedding** — "Embedding reference" · reference · app-dev · 4318w · 11 inbound
    - The canonical JWT reference for every embed type — dashboard, chart, aiAgent, metricsCatalog, dataApp, and apiAccess tokens, each with full TypeScript shape; the writeActions claim (service account vs. named-user actor); allowed-content lists; user attributes for row/column-level security; token expiration guidance; and code examples in Node.js, Python, Ruby, and Java.
    - ⚠ overlaps: references/iframe-embedding and references/react-sdk (both repeat JWT/token examples for their method); guides/embedding/dashboards, charts, ai-agents, metrics-catalog, data-apps (each guide re-documents its own token's fields and a working example) — this is an intentional reference-vs-how-to split, see systemic note below.
  - **references/iframe-embedding** — "Embedding with iframe" · reference · app-dev · 2239w · 7 inbound
    - iframe-specific reference — URL patterns for dashboard (`/embed/{projectUuid}#{jwt}`) and standalone data app (`/embed/{projectUuid}/app/{appUuid}#{jwt}`) embeds; URL query params (theme, backgroundColor, and the beta-gated timezone param); HTML embedding patterns (responsive wrappers, sandbox attribute tradeoffs); SPA/CMS/SSR code patterns; token-refresh strategies; and a troubleshooting section (token errors, blank iframe, CORS, URL encoding).
    - ⚠ overlaps: references/embedding (URL/JWT construction examples repeated); guides/embedding/dashboards (its "iframe embedding" section is a condensed duplicate of this reference's URL/theming content); guides/embedding/data-apps (URL pattern duplicated exactly).
  - **references/react-sdk** — "Embedding with React SDK" · reference · app-dev · 5095w · 9 inbound
    - Full `@lightdash/sdk` component/prop reference — Dashboard, DashboardBuilder, Chart, Explore, AiAgent, and MetricsCatalog components, plus the useLightdashContent and useLightdashAiAgentThreads hooks; CORS setup requirement; styling (styles prop plus a CSS class-override table like ld-dashboard-header); theme prop and color palettes; dashboard filtering via SdkFilter/FilterOperator; and localization via contentOverrides plus the CLI's `--language-map` flag.
    - ⚠ overlaps: references/embedding (JWT examples per component repeated); guides/embedding/dashboards, charts, ai-agents, metrics-catalog (each repeats a subset of that component's props and a basic-usage example) — same intentional reference/guide split as above.

### Self-hosting and deployment

- **self-host/self-host-lightdash** — "Self-Host Lightdash" · how-to · platform · 805w · 2 inbound
  - Kubernetes+Helm deployment walkthrough — add the helm repo, create a namespace, build a minimal values.yaml (LIGHTDASH_SECRET, S3_* vars, service.type, SITE_URL), then `helm install` or `helm template` + `kubectl apply`. Closes with a categorized list of links (required/recommended/optional) to every customize-deployment page for production hardening.
  - ⚠ overlaps: self-host/production-deployment-checklist — the same core values.yaml fields (LIGHTDASH_SECRET, SITE_URL, S3 required-for-boot) are re-explained there in more depth
- **self-host/lightdash-cloud-vs-self-hosted** — "Lightdash Cloud vs. Self-Hosted" · explanation · platform · 227w · 2 inbound
  - Short decision framework for choosing Cloud vs. self-hosting — enterprise POC while infosec reviews Cloud, hobbyist/customization use cases — with a pointer to Enterprise License Keys for self-hosted Enterprise features.
- **self-host/production-deployment-checklist** — "Production deployment checklist" · mixed(reference+how-to) · platform · 2755w · 0 inbound
  - Three-tier (PoC / scalable production / optional-observability) Helm production checklist with an architecture component table and copy-pasteable values.yaml snippets for core config, headless browser tuning, scheduler worker, sizing/HA (with a resource-request table per component), Postgres (pgvector, connection budget, TLS, migration job), object storage (bucket-per-purpose lifecycle rules), email deliverability, load balancer settings, SSO-only auth policy, CSP/CORS hardening, and Enterprise feature flags (caching, embedding, AI Analyst, data apps/sandboxes).
  - ⚠ overlaps: self-host/self-host-lightdash — restates the same required values.yaml fields | self-host/enterprise-on-prem — duplicate UAT-rehearsal/rollout guidance | self-host/customize-deployment/enable-headless-browser-for-lightdash — recommends browserless image tag `v2.49.0` as "what Lightdash Cloud runs," conflicting with that page's `v2.24.3` claim | self-host/customize-deployment/recommended-resources — its sizing table gives scheduler CPU `500m` vs. that page's `1` | self-host/nats-workers/overview, self-host/customize-deployment/sandboxes, self-host/customize-deployment/enterprise-license-keys, self-host/customize-deployment/configure-prometheus-metrics-for-self-hosted-lightdash, self-host/customize-deployment/configure-logging-for-lightdash — each condensed into a subsection here and cross-linked to the full page | guides/data-apps/self-hosting — Data apps production checklist subsection duplicates that page's config
  - 🚩 flags: none — functions intentionally as a synthesis/index page, though see the version and CPU conflicts noted above
- **self-host/enterprise-on-prem** — "Enterprise on-prem setup" · how-to · platform · 509w · 0 inbound
  - FedRAMP-ready Enterprise on-prem setup — requires Postgres with `pgvector`; pull a pinned DockerHub image (no LTS tag exists); recommends a UAT/pre-prod rollout mirroring prod config and a refreshed prod DB replica before promoting; license key via `LIGHTDASH_LICENSE_KEY` validated against `api.keygen.sh`; table mapping each Enterprise feature (custom roles, AI agents, multi-agent Slack, caching, embedding, MCP) to its enabling env var; OSS→Enterprise migration steps (schema upgrade races across containers).
  - ⚠ overlaps: self-host/production-deployment-checklist — near-identical "pin image tag, rehearse upgrades on a UAT instance with a refreshed DB replica" guidance appears in both pages' Upgrades sections | self-host/customize-deployment/enterprise-license-keys — license key configuration and the `api.keygen.sh` network requirement are explained fully in both places
- **self-host/self-host-lightdash-docker-compose** — "Self-host Lightdash using docker compose" · how-to · platform · 241w · 2 inbound
  - Minimal local proof-of-concept setup — clone the lightdash repo, edit `.env` (PGHOST/PGUSER/DBT_DEMO_DIR etc.), export LIGHTDASH_SECRET and PGPASSWORD, then `docker compose up`. Includes a Windows-specific Docker daemon timeout fix.
  - ⚠ overlaps: self-host/customize-deployment/configure-standalone-scheduled-worker — has a different, fuller docker-compose.yml (separate scheduler service) that could confuse readers comparing the two compose files
- **self-host/update-lightdash** — "Updating Lightdash to the latest version" · how-to · platform · 79w · 1 inbound
  - Two mechanisms only — `docker pull` + restart for local Docker installs, or `helm repo update` + `helm upgrade` for Kubernetes. No version-pinning, rollback, or migration-safety guidance.
  - ⚠ overlaps: self-host/upgrading-lightdash-versioning — versioning policy split into a separate page from these upgrade mechanics | self-host/production-deployment-checklist — Upgrades and operations section covers the same `helm upgrade` path plus the migration-job hook and rollback rules this page omits entirely
  - 🚩 flags: stub — 79 words, no mention of pinning versions, rollback, or the migration job that production-deployment-checklist treats as essential
- **self-host/upgrading-lightdash-versioning** — "Upgrading Lightdash (versioning)" · reference · platform · 151w · 1 inbound
  - Explains Lightdash's semver policy (patch=safe, minor=review release notes/may be backwards-incompatible, major=read upgrade guide first). Its only "Upgrade guides" entry, Version 0→1, says "Coming soon."
  - ⚠ overlaps: self-host/update-lightdash — upgrade mechanics live on that separate page | self-host/production-deployment-checklist — repeats the patch/minor/major cadence advice inline under Upgrades and operations
  - 🚩 flags: stub — the one linked upgrade guide is an empty "Coming soon" placeholder
- **NATS workers**
  - **self-host/nats-workers/overview** — "NATS workers" · reference · platform · 703w · 3 inbound
    - Explains the opt-in NATS JetStream architecture that offloads query execution from the API server to dedicated worker pods (warehouse workers, pre-aggregate workers); requires Helm chart 2.7.2+ and Lightdash 0.2675.0+; memory-store vs. file-store tradeoffs; explicit warning to never set `nats.enabled: true` without `warehouseNatsWorker.enabled: true` or queries stall.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — that page's "NATS worker" section describes `NATS_ENABLED` as routing *scheduler jobs* instead of Postgres polling, directly contradicting this page's description of NATS as query-execution offload; one of the two is stale
    - 🚩 flags: none — this page is the internally-consistent, detailed source; see the conflict flagged on environment-variables
  - **self-host/nats-workers/warehouse-workers** — "Warehouse workers" · reference · platform · 445w · 2 inbound
    - Config reference for `warehouseNatsWorker` Helm values (replicas, concurrency→`NATS_WORKER_CONCURRENCY`, CPU/memory requests) plus troubleshooting for query timeouts, OOM kills, and NATS connection errors.
    - ⚠ overlaps: self-host/nats-workers/overview — repeats the same `NATS_ENABLED`/`NATS_URL` auto-configured table | self-host/nats-workers/pre-aggregate-workers — shares an identical example nats+warehouseNatsWorker YAML block
  - **self-host/nats-workers/pre-aggregate-workers** — "Pre-aggregate workers" · reference · platform · 1278w · 2 inbound
    - Enterprise-only pre-aggregate worker config — handles both scheduled materializations (warehouse→S3) and DuckDB query serving from materialized Parquet; requires a dedicated S3 bucket with `s3:PutObject` permission separate from the main results bucket; DuckDB memory tuning via `PRE_AGGREGATE_DUCKDB_QUERY_MEMORY_LIMIT`; troubleshooting for materialization failures and OOM kills.
    - ⚠ overlaps: self-host/nats-workers/warehouse-workers — duplicate nats+warehouseNatsWorker config block | self-host/customize-deployment/environment-variables — same `PRE_AGGREGATE_*` and `PRE_AGGREGATES_*` vars listed in its Pre-aggregates section
- **Customize deployment**
  - **self-host/customize-deployment/enterprise-license-keys** — "Enterprise License Keys" · how-to · platform · 515w · 8 inbound
    - Contact the enterprise team for a key, set `LIGHTDASH_LICENSE_KEY` via env var / Helm `secrets` / docker-compose; license is validated against `https://api.keygen.sh` on every server start (must be network-reachable, the only required outbound Enterprise call); verify via Settings → Organization badge; troubleshooting checklist.
    - ⚠ overlaps: self-host/enterprise-on-prem — duplicates the same license-key setup steps and `api.keygen.sh` network requirement | self-host/production-deployment-checklist — Enterprise features section repeats the same network note
  - **self-host/customize-deployment/configure-mcp-for-lightdash** — "Configure MCP for self-hosted Lightdash" · how-to · platform · 390w · 3 inbound
    - Enterprise-only `MCP_ENABLED=true` env var exposes `/api/v1/mcp` with a built-in OAuth server (no external OAuth config needed); `MCP_RUN_SQL_MAX_LIMIT` caps rows independently of the AI Analyst limit; example `claude mcp add` command; troubleshooting table for 404s/OAuth failures.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — same two vars in its MCP section
  - **self-host/customize-deployment/configure-a-slack-app-for-lightdash** — "Configure a Slack integration for self-hosted Lightdash" · how-to · platform · 592w · 2 inbound
    - Create a Slack app from a manifest (bot scopes, unfurl domains, OAuth redirect URLs, event subscription URL), copy Client ID/Secret/Signing Secret into `SLACK_CLIENT_ID`/`SLACK_CLIENT_SECRET`/`SLACK_SIGNING_SECRET`/`SLACK_STATE_SECRET`; alternative Socket Mode setup (`SLACK_APP_TOKEN`, `SLACK_SOCKET_MODE`) for instances not reachable from the public internet.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — same four core vars listed again in its Slack Integration section, which also documents `SLACK_PORT`, `SLACK_CHANNELS_CACHED_TIME`, and multi-agent-channel vars not mentioned on this page
  - **self-host/customize-deployment/configure-github-for-lightdash** — "Configure a Github integration for self-hosted Lightdash" · how-to · platform · 352w · 2 inbound
    - Create a GitHub App (callback URL `/api/v1/github/oauth/callback`, repo permissions checks/contents/pull-requests read-write, deactivate webhooks), generate a client secret and base64-encoded private key, set `GITHUB_PRIVATE_KEY`/`GITHUB_CLIENT_SECRET`/`GITHUB_CLIENT_ID`/`GITHUB_APP_ID`/`GITHUB_APP_NAME`.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — its GitHub Integration table also documents `GITHUB_REDIRECT_DOMAIN` and `GITHUB_WEBHOOK_SECRET`, neither mentioned here, despite this page instructing readers to deactivate webhooks
  - **self-host/customize-deployment/configure-lightdash-to-use-an-external-database** — "Configure Lightdash to use an external database" · how-to · platform · 223w · 2 inbound
    - `postgresql.enabled: false` + `externalDatabase` host/port/user/password/database values.yaml block; requires the `uuid-ossp` Postgres extension; migrations run automatically, with a note about releasing the `knex_migrations_lock` table if a migration hangs.
    - ⚠ overlaps: self-host/production-deployment-checklist — its PostgreSQL section adds the `pgvector` extension requirement (for Enterprise AI features), HA/backup guidance, connection-pool sizing (`PGMAXCONNECTIONS`), and TLS config that this page doesn't mention at all
    - 🚩 flags: none — but see the systemic pgvector gap noted below
  - **self-host/customize-deployment/configure-lightdash-to-use-external-object-storage** — "Configure Lightdash to use external storage" · how-to · platform · 661w · 6 inbound
    - Step-by-step bucket creation for GCP (Interoperability HMAC keys), AWS (IAM access keys), and MinIO (path-style URLs); explains Azure Blob Storage isn't S3-compatible and requires MinIO or s3proxy as a shim; required env vars `S3_ENDPOINT`/`S3_REGION`/`S3_BUCKET`/`S3_ACCESS_KEY`/`S3_SECRET_KEY`; IAM role auth caps signed URLs at 7 days.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — its S3 section adds `S3_PUBLIC_ENDPOINT`, `S3_FORCE_PATH_STYLE`, and the `RESULTS_S3_*` bucket-override vars not covered here | self-host/production-deployment-checklist — Object storage section adds the bucket-per-purpose/lifecycle-rule strategy this page doesn't address
  - **self-host/customize-deployment/configure-logging-for-lightdash** — "Configure logging for Lightdash" · reference · platform · 237w · 2 inbound
    - Small table of `LIGHTDASH_LOG_*` env vars (level, format, outputs, per-console/per-file overrides, and `LIGHTDASH_LOG_AUDIT_ACTOR_AS_STRING` for log indexers that map `actor` as text).
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — this exact table is repeated verbatim in the Logging section there
  - **self-host/customize-deployment/configure-prometheus-metrics-for-self-hosted-lightdash** — "Configure Prometheus metrics for self-hosted Lightdash" · reference · platform · 1996w · 2 inbound
    - Full metrics catalog (process, Node.js, Postgres pool, queue, query, pre-aggregate, AI agent, S3, and OpenTelemetry HTTP-server metrics) with types/labels; OpenTelemetry Collector example config; links for setting up Prometheus on GCP/AWS.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — its Prometheus section lists the same core config vars but also documents `LIGHTDASH_PROMETHEUS_EVENT_METRICS_ENABLED`, `_EXTENDED_METRICS_ENABLED`, and `_ALL_QUERY_METRICS_ENABLED`, none of which appear on this dedicated page
    - 🚩 flags: outdated — missing three Prometheus feature-flag env vars that exist in the environment-variables reference, suggesting this page wasn't updated when those flags shipped
  - **self-host/customize-deployment/configure-smtp-for-lightdash-email-notifications** — "Configure SMTP for Lightdash email notifications" · reference · platform · 224w · 6 inbound
    - SMTP env var table — host/port/secure/user/password-or-access-token/allow-invalid-cert/sender email+name/inline-CID image embedding for firewalled deployments.
    - ⚠ overlaps: self-host/customize-deployment/environment-variables — near-identical table, except it also documents `EMAIL_SMTP_USE_AUTH` (for unauthenticated internal relays), missing from this dedicated page
    - 🚩 flags: outdated — missing `EMAIL_SMTP_USE_AUTH`, which production-deployment-checklist's Email deliverability section explicitly recommends setting
  - **self-host/customize-deployment/configure-standalone-scheduled-worker** — "Configure a standalone scheduler worker for self-hosted Lightdash" · how-to · platform · 204w · 0 inbound
    - Decouple scheduled-delivery processing from the main API server — set `SCHEDULER_ENABLED=false` on the main server, run `yarn workspace backend scheduler` as its own process (same env vars as main server); full docker-compose.yml example with a separate `scheduler` service.
    - ⚠ overlaps: self-host/customize-deployment/enable-scheduler-in-self-hosted-lightdash — that page's title implies it's where scheduler setup lives, but it's a content-free stub while this page has the real instructions | self-host/customize-deployment/environment-variables — Scheduler section adds `SCHEDULER_CONCURRENCY`/`SCHEDULER_JOB_TIMEOUT`/`SCHEDULER_POLL_INTERVAL` tuning not mentioned here
    - 🚩 flags: naming — "standalone scheduled worker" vs. the separate "Scheduler" page creates two near-synonymous titles for one topic, and the more substantive content sits under the less discoverable name
  - **self-host/customize-deployment/enable-headless-browser-for-lightdash** — "Add headless browser on self-hosting" · how-to · platform · 949w · 3 inbound
    - Explains the Playwright/Browserless screenshot architecture (socket connection to `HEADLESS_BROWSER_HOST:PORT`, renders `SITE_URL`, stores to S3), recommends `ghcr.io/browserless/chromium:v2.24.3`; two-layer timeout tuning (Browserless container `TIMEOUT` vs. Lightdash's `HEADLESS_BROWSER_MAX_SCREENSHOT_RETRIES`/`RETRY_BASE_DELAY_MS`/`SCHEDULER_JOB_TIMEOUT`); ordered troubleshooting steps for large-dashboard export failures; a fully-internal-HTTPS setup using `INTERNAL_LIGHTDASH_HOST_IGNORE_HTTPS_ERRORS` for self-signed certs, with the HSTS-pinning failure mode explained.
    - ⚠ overlaps: self-host/production-deployment-checklist — recommends browserless `v2.49.0` as "what Lightdash Cloud runs," directly conflicting with this page's `v2.24.3` claim about the same thing | self-host/customize-deployment/environment-variables — same headless-browser vars restated
    - 🚩 flags: outdated — the Browserless image version this page claims Lightdash Cloud runs (`v2.24.3`) contradicts production-deployment-checklist's claim (`v2.49.0`) for the same fact; one is stale
  - **self-host/customize-deployment/enable-scheduler-in-self-hosted-lightdash** — "Enable the scheduler in self-hosted Lightdash" · how-to · platform · 30w · 1 inbound
    - Page body is a single line, "Coming soon" — no content beyond the intro banner.
    - ⚠ overlaps: self-host/customize-deployment/configure-standalone-scheduled-worker — the actual scheduler-decoupling instructions live there instead | self-host/customize-deployment/environment-variables — `SCHEDULER_ENABLED` (default `true`) is documented there, not here
    - 🚩 flags: stub — entire body is "Coming soon"; also naming, since the scheduler runs by default and needs no "enabling," so the title promises content that doesn't map to any real task
  - **self-host/customize-deployment/environment-variables** — "Environment variables" · reference · platform · 9579w · 16 inbound
    - The master ~9,600-word env-var reference spanning Postgres, server/networking, query limits, scheduler, headless browser, PATs, timezone, soft delete, feature toggles/flags, SMTP, SSO (Okta/Google/OneLogin/Azure AD/OIDC/Databricks/Snowflake), S3, persistent download URLs, cache, logging, Prometheus, security (CSP/CORS), AI Analyst (OpenAI/Anthropic/Azure/OpenRouter/Bedrock, including LLM-gateway and Zero-Data-Retention config), Slack/GitHub/GitLab/Teams/GCP integrations, embedding, custom roles, service accounts, SCIM, Sentry, sandboxes/data-apps runtime, managed agent, MCP, the Metrics SQL API (pgwire/TLS), NATS worker, and instance auto-initialization (`LD_SETUP_*` org/project/user-attribute/group-access bootstrapping schema).
    - ⚠ overlaps: self-host/customize-deployment/configure-smtp-for-lightdash-email-notifications, configure-logging-for-lightdash, configure-prometheus-metrics-for-self-hosted-lightdash, configure-mcp-for-lightdash, configure-a-slack-app-for-lightdash, configure-github-for-lightdash, enterprise-license-keys, use-sso-login-for-self-hosted-lightdash, enable-headless-browser-for-lightdash, sandboxes, self-host/nats-workers/overview — each of these dedicated pages duplicates a subset of this page's tables, with several now out of sync (see flags below)
    - 🚩 flags: outdated — the "NATS worker" section describes `NATS_ENABLED` as enabling a scheduler job queue ("publishes scheduler jobs to NATS instead of polling Postgres"), which contradicts self-host/nats-workers/overview.mdx's description of NATS as offloading warehouse/pre-aggregate query execution, with no scheduler-job mechanism mentioned there at all — one of the two descriptions is stale
  - **self-host/customize-deployment/google-sheets-integration** — "Configure a Google Sheets integration for self-hosted Lightdash" · how-to · platform · 172w · 1 inbound
    - Enable Google SSO first (or set `AUTH_GOOGLE_ENABLED=false` to keep Sheets OAuth working without exposing Google as a login option); create a Google Drive Picker API key (enable Picker API + Sheets API in GCP) and store it as `GOOGLE_DRIVE_API_KEY`.
    - ⚠ overlaps: self-host/customize-deployment/use-sso-login-for-self-hosted-lightdash — Google SSO setup this page depends on | self-host/customize-deployment/environment-variables — `GOOGLE_DRIVE_API_KEY` and `AUTH_GOOGLE_ENABLED` listed again in its Google Cloud Platform section
  - **self-host/customize-deployment/recommended-resources** — "Resource recommendations" · reference · platform · 36w · 2 inbound
    - A bare CPU/Memory/Ephemeral-storage table for three components (lightdash, browserlessChrome, scheduler) with zero context — no Kubernetes framing, no explanation of when the numbers apply or how they interact with replica counts.
    - ⚠ overlaps: self-host/production-deployment-checklist — its Sizing and availability table covers the same three-plus components but gives scheduler CPU as `500m`, conflicting with this page's `1`
    - 🚩 flags: outdated — scheduler CPU recommendation (`1`) disagrees with production-deployment-checklist's more detailed sizing table (`500m`)
  - **self-host/customize-deployment/sandboxes** — "Sandboxes" · reference · platform · 3079w · 1 inbound
    - Explains why AI writeback and data-app generation run untrusted, agent-generated (Claude Code) code inside disposable sandboxes rather than on the Lightdash server; details five pluggable `SANDBOX_PROVIDER` backends — E2B (managed default), AWS Lambda MicroVMs, Azure Container Apps Sandboxes, Google Cloud Run Sandboxes (preview, data apps only), and local Docker (dev-only, refuses to start under `NODE_ENV=production`) — each with prerequisites, config env vars, and network-egress lockdown guidance; snapshot/idle-timeout lifecycle vars; full env var reference table.
    - ⚠ overlaps: guides/data-apps/self-hosting and guides/developer/ai-writeback-self-hosting (outside this batch) — both cover feature-specific sandbox/E2B setup that duplicates this page's provider-selection and E2B config content, though this page is the correct general/shared home for provider choice while those two are appropriately narrower on model/cost config | self-host/customize-deployment/environment-variables — Data apps runtime section repeats `APPS_RUNTIME_ENABLED`/`E2B_*` vars
  - **self-host/customize-deployment/secure-lightdash-with-https** — "Secure Lightdash with HTTPS" · how-to · platform · 85w · 2 inbound
    - A bare values.yaml snippet — `SITE_URL` (https), `SECURE_COOKIES: true`, `TRUST_PROXY: true`, `service.type`, and an example ingress/TLS block — with no prose explaining what any of these settings actually do or why both cookie/proxy flags are needed together.
    - ⚠ overlaps: self-host/production-deployment-checklist — Core configuration and Load balancer sections explain the same `SECURE_COOKIES`/`TRUST_PROXY` pairing with the missing rationale, plus timeout/health-check requirements this page never mentions
    - 🚩 flags: stub — 85 words, pure config snippet with no explanation, while the same settings are explained properly elsewhere
  - **self-host/customize-deployment/use-sso-login-for-self-hosted-lightdash** — "Configure Lightdash to use passwords or SSO for authentication" · how-to · platform · 2135w · 8 inbound
    - Full self-hosted SSO setup for Okta (incl. custom-authorization-server groups claim), Google (incl. combining BigQuery-SSO consent into one screen via `AUTH_GOOGLE_INCLUDE_BIGQUERY_SCOPE`), OneLogin, Azure AD, and generic OIDC (incl. `private_key_jwt` auth) — each with IdP-side app creation steps and the resulting env vars; a CLI command to override a user's password directly in the container; a deprecated Okta-group-auto-assignment flag pointing readers to SCIM instead. Repeatedly notes that Lightdash Cloud users should use the Organization Settings UI instead of these env vars.
    - ⚠ overlaps: references/workspace/configure-sso (outside batch) — the Cloud-side equivalent, explicitly and appropriately cross-referenced throughout rather than duplicated | self-host/customize-deployment/environment-variables — same full SSO var list per provider, appropriately split between how-to steps (here) and flat reference (there) | self-host/production-deployment-checklist — Authentication policy section recommends SSO-only + PAT policy at a higher altitude

### Contact

- **contact/faqs** — "FAQs" · reference · explorer · 0w · 0 inbound
  - Redirect-only stub — frontmatter sets `url: https://lightdash.help.usepylon.com/` with zero body content; exists purely to forward visitors to an external Pylon help desk.
  - ⚠ overlaps: contact/contact-info — see above (conflicting FAQ destination)
  - 🚩 flags: stub — no body content, pure external redirect
- **contact/contact-info** — "Contact Us" · reference · explorer · 329w · 2 inbound
  - Directory of contact channels by intent: bug/feature reports (GitHub issues), product feedback (GitHub issues or email), staying updated (Slack community, GitHub star, newsletter, dbt Slack's #tools-lightdash), billing/invoice requests (email support), and troubleshooting help — which points to "our FAQs" at a GitHub Discussions Q&A category, plus Slack, in-app help, GitHub issues, and email.
  - ⚠ overlaps: contact/faqs — both are the "get help" entries in the same nav group, but each points to a different FAQ destination (GitHub Discussions here vs. an external help desk in faqs.mdx)
  - 🚩 flags: outdated — links to `github.com/lightdash/lightdash/discussions/categories/q-a` as "our FAQs," while the sibling faqs.mdx page instead redirects to `lightdash.help.usepylon.com`, suggesting the FAQ/support destination moved and this page wasn't updated to match
- **contact/generating-har-file** — "Generating a HAR file" · how-to · explorer · 727w · 0 inbound
  - Per-browser (Chrome, Firefox, Safari, Edge) steps to capture a HAR file for Lightdash support, a security warning about sensitive session/cookie data in HAR files with a recommended sanitizer tool, and instructions for compressing and sending the file (with a size-limit fallback note).

## dbt guides

- **dbt-guides/dbt-package-lock** — "Guide to package-lock.yml in dbt" · explanation · builder · 879w · 0 inbound
  - Explains what dbt's `package-lock.yml` is (auto-generated by `dbt deps`), semantic versioning and version ranges in `packages.yml`, why pinning matters (reproducible builds, team consistency, controlled updates), the file's structure including the `sha1_hash` integrity fingerprint, why to commit it to git, and how to regenerate/troubleshoot it.
  - 🚩 flags: misplaced — pure dbt-core conceptual content with zero Lightdash-specific mentions, sitting in "dbt-guides" alongside otherwise Lightdash-specific migration guides (1.10, Fusion); reads like a generic dbt blog post rather than product documentation
- **dbt-guides/dbt-1.10-migration** — "Migrating to dbt 1.10: Fix Meta and Tags Configuration Warnings" · how-to · builder · 1177w · 0 inbound
  - Explains the dbt 1.10 deprecation warnings for top-level `meta`/`tags` (plus `freshness`, `docs`, `group`, `access`) moving under a `config:` block, shows before/after YAML at model/column/source level, and walks through Lightdash's own MetaMove CLI (`pipx install metamove`, transform-to-`transformed`-dir-first safety workflow, `-i` in-place, `-o` custom output) plus manual migration steps and troubleshooting.
  - ⚠ overlaps: dbt-guides/dbt-fusion-migration — step 1 of that guide re-explains and links back to this same MetaMove migration
- **dbt-guides/dbt-fusion-migration** — "Migrating to dbt Fusion" · how-to · builder · 330w · 0 inbound
  - Describes dbt Fusion (Rust rewrite, faster, native SQL understanding, replaces dbt Core/Cloud engine) and a two-step migration path: upgrade to dbt 1.10 and resolve all deprecation warnings first (via MetaMove + dbt-autofix), then dbt Core users manually install/run the Fusion engine (dbt Cloud users get migrated automatically).
  - ⚠ overlaps: dbt-guides/dbt-1.10-migration — step 1 duplicates/links back to that page's MetaMove content
  - 🚩 flags: stub — ~230 words across two numbered steps, mostly linking to external dbt docs; doesn't say whether Lightdash's own CLI (`lightdash generate`, `lightdash deploy`) is verified compatible with Fusion, a notable gap given how CLI-centric the rest of this batch is

## API

### API

- **api-reference/v1/introduction** — "Lightdash API" · reference · app-dev · 49w · 4 inbound
  - One-paragraph landing page for the auto-generated OpenAPI reference of all public Lightdash API endpoints; points to Personal Access Token setup as the prerequisite before calling anything.
  - ⚠ overlaps: none (referenced-as-prerequisite by nearly every API-facing page, not duplicated content)
  - 🚩 flags: stub — 3 sentences total, purely a redirect/landing page with a support-email line and a PAT link
- **api-reference/v1/recipes** — "Recipes" · how-to · app-dev · 1351w · 2 inbound
  - Five copy-paste API workflows chaining multiple endpoints: extract compiled SQL from a saved chart (fetch chart → extract `metricQuery` → POST to `compileQuery`); a Python dashboard-cleanup/usage-audit script (v2 Content API, exports CSV/Excel/JSON); exporting all org users via `/api/v1/org/users`; bulk-assigning project roles from a CSV via a Python script; and filtering metrics by dbt/Lightdash-YAML field tag through `/api/v2/.../dataCatalog/metrics-with-time-dimensions` (with dbt 1.9 vs. 1.10+/Fusion vs. Lightdash YAML tag-syntax tabs and a full query-parameter table).
  - ⚠ overlaps: sdk/python-sdk — the chart-SQL-extraction recipe and the SDK's `client.sql()` both solve "get SQL out of Lightdash programmatically" via different mechanisms; dbt-guides/dbt-1.10-migration — the meta/tags tab example repeats syntax shown there

### Reference

- Auto-generated from [`swagger.json`](https://raw.githubusercontent.com/lightdash/lightdash/refs/heads/main/packages/backend/src/generated/swagger.json) (OpenAPI reference); not audited page-by-page.

## Not in navigation — the orphans/snippets

- **snippets/embedding-availability** (snippet) — "" · 25w · 0 inbound
  - One-line `<Info>` callout stating embedding is available to Lightdash Cloud and Enterprise On-Prem customers, with a typeform link to request access. Included by: guides/embedding/dashboards.mdx, guides/embedding/how-to-embed-content.mdx, guides/embedding/data-apps.mdx, guides/embedding/metrics-catalog.mdx, guides/embedding/charts.mdx, guides/embedding/ai-agents.mdx, references/embedding.mdx, references/iframe-embedding.mdx.
- **snippets/liquid-templating** (snippet) — "" · 184w · 0 inbound
  - Reference table of Liquid template tags (`${ value.formatted }`, `${ value.raw }`, `${ row.table.column.formatted }`, `${ row.table.column.raw }`) and filters (`url_encode`, `downcase`, `append`) usable for runtime value templating, with a link to the full Liquid filter docs. Included by: references/dimensions.mdx, references/metrics.mdx.
- **timezones-draft** (orphan draft) — "Working with timezones in Lightdash" · 3594w · 0 inbound
  - Unpublished (`hidden: true`) draft covering warehouse timezone modeling (timezone-aware column types per warehouse, DATE vs. TIMESTAMP semantics, `wall_clock_timezone`/`convert_timezone: false` dbt column annotations) and day-to-day chart behavior (resolved-timezone badge, relative vs. absolute date filters, day-vs-sub-day bucket grouping across viewers, pinned vs. "each viewer's own timezone" chart sharing, profile timezone, scheduled-delivery time vs. data timezone, embedded-chart timezone fallback, `${ldQueryTimezone}` in custom SQL), plus a troubleshooting table and a four-scenario worked example.
  - ⚠ overlaps: guides/date-zoom — both touch chart time granularity, but from different angles (interactive granularity switching vs. which timezone a chart resolves to) — not substantive duplication
  - 🚩 flags: outdated — explicitly a draft, not live; riddled with inline "— [GLITCH-NNN]" Linear-ticket review markers, a section headed "What if I don't have timezones in my columns? [NEEDS REWRITING - TOO CLAUDE]", two sections marked "[FLAGGED FOR REVIEW]", and an appendix titled "Appendix — Worked example... [DRAFT VERY CLAUDE]" — none of this internal scaffolding is meant to reach a published page
- **snippets/checklist.jsx** (dead code) — "?" · ?w · ? inbound
  - Reusable `<Checklist>`/`<ChecklistItem>` React component pair — renders a sticky progress bar (X/Y done, %) with a Reset button, and interactive checkbox list items whose checked state persists per-reader in `localStorage` under a caller-supplied `storageKey`.
  - 🚩 flags: stub — grepped across the entire `.mdx` tree for `<Checklist` and `from '/snippets/checklist'`; zero pages import or render the component. Three files matched a plain-text search for the word "checklist" (timezones-draft.mdx's "TL;DR checklist" heading, guides/ai-agents/autopilot.mdx, self-host/production-deployment-checklist.mdx) but none actually use the component — it appears to be fully orphaned/dead code.

## Nav anomalies

- `references/integrations/lightdash-mcp` appears in two groups — "Build with AI" and "Integrations".

## Existing redirects

- 65 redirects are defined. `docs.json`'s top-level `redirects` array is the redirect mechanism — see `docs.json` for the full source → destination list.
