---
title: "Visualizations"
description: "Every chart type available in Lightdash and when to reach for each one"
doc-type: reference
---

Lightdash ships with **built-in chart types** — bar, line, pie, table, and more — that cover most use cases. Each built-in chart has its own configuration options you can adjust in the **Configure** sidebar.

When the built-ins aren't enough, reach for a [custom chart](/visualizations/custom-charts): install one from the [chart library](/visualizations/chart-library), build a reusable one in [Chart Studio](/visualizations/chart-studio), or write a one-off spec [with Vega-Lite](/visualizations/custom-charts#vega-lite-charts).

## Picking a chart type

To change how your data is displayed, click **Configure** when you're querying from a table. The **Configure chart** sidebar opens on the right. Click **Change** next to the current chart type to open the chart type gallery, then pick a card:

<Frame>
  <img
    alt="The Configure chart sidebar in the Explorer with the chart type gallery open, listing custom chart types above the built-in ones"
    className="block dark:hidden"
    src="/images/visualizations/select-chart-type-light.png"
  />
  <img
    alt="The Configure chart sidebar in the Explorer with the chart type gallery open, listing custom chart types above the built-in ones"
    className="hidden dark:block"
    src="/images/visualizations/select-chart-type-dark.png"
  />
</Frame>
